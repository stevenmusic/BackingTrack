# BackingTrack 專案規則

## 和弦詞彙
- `QUALITY` 表是對著 HarmonyMap 的和弦資料庫生出來的：它的 80 種和弦類型、
  連同每種的別名寫法，共 134 種拼法。HarmonyMap 加了新和弦，這裡要跟著補，
  兩個工具看得懂的東西必須是同一套
- 兩個不收：「So What」有空白當不了 token；「♯5」跟 `C♯5` 撞在一起，一律當後者
- `normalizeSymbol()` 已經吃得下爵士表上的寫法：`∆ Δ △` → maj、`° ◦` → dim、
  `−`(U+2212) → m，全形英數也先轉半形。`min7` / `M7` / `ø` / `o7` 本來就在表裡
- **半形的 `-` 是特例**：它同時是小節分隔符（`C - Am - F - G`），所以只在
  「根音後面緊接著 `-`，再接數字或結尾」時才當小三和弦（`C-7` → `Cm7`、`Bb-` → `Bbm`）。
  `C-Am`（後面接音名）跟 `C - Am`（有空白）還是分隔符。這條在 `parseProgression`
  切 token **之前**做，切完就來不及了
- 轉位（slash chord）本來就完整支援：`C/E`、`Am7/G`、`C6/9/E` 都吃得下，
  貝斯與鋼琴左手彈的是斜線後面那個音（`chord.bassPc`），不是根音

## 音源
三軌都在 sfzinstruments 這個 org 底下（跟鋼琴同一家），要換之前先問：
- 鋼琴：Salamander Grand，跟 ScrollScore 指向同一組檔案
- 鼓：Virtuosity Drums（Versilian Studios × Karoryfer，KVRDC'21）。
  這是波士頓實錄的**爵士鼓組**（鼓手 Austin McMahon），不是管弦打擊樂。
  取 `Samples/mid/`（中距離麥克風）那一組
- 貝斯**兩把,照感覺換**(`BASS_KITS` / `bassKit()`):
  - 直(流行搖滾)→ Black And Blue Basses 的 `darkblack` 指彈電貝斯,mf 力度
  - 拉丁與搖擺 → D. Smolken 的 1958 Otto Rubner **低音提琴**撥弦(`dsmolken.double-bass`
    的 `pizz/`,m 力度、round robin 取 a)。爵士的走動低音本來就是低音提琴,
    bossa 也是(Getz/Gilberto 那個年代就是尼龍弦吉他加低音提琴)
  - 兩把都是 D. Smolken 錄的,音色調性一致;**檔名的音高寫法不一樣**:
    電貝斯是 `midi = 八度 × 12 + 音級`(`e2` = 28),低音提琴是科學音名
    (`pizz_g1` = 31,sfz 裡的 `pitch_keycenter` 就是這個數字)。不要混用
  - buffer 分兩個 Map 收(`bassBuffers.electric` / `.upright`),
    `playBassNote` 只從現在這把裡挑最近的音——混在一起會抓到另一把的取樣

VCSL 看起來像是鼓，其實是管弦打擊樂（定音鼓、大鑼、碰鈸），不要拿來當爵士鼓用。

每一份檔案都走 `fetchDecode()` 的多來源清單：jsDelivr →
raw.githubusercontent（鋼琴多一條 tonejs.github.io）。同一份檔案不同路由，
新增音源時照這個模式走，不要只寫一個網址。

## 兩個軸:感覺先由和弦決定,疏密歸使用者
不要把「曲風」做成**必選**的選單。一開始讓使用者直接挑 Bossa/Pop/Rock/Smooth Jazz,
結果是 8 組進行 × 4 種風格裡一堆說不通的組合(Dm9-G13-Cmaj9 套 Rock、
C-F-G-F 套 Bossa)。「要用哪種感覺」本來就是和弦決定的,所以**預設是自動**;
選單只是覆寫,不是入口。

- **感覺**(`FEELS`,預設自動):`detectFeel()` 看和弦語彙——任一顆有九度以上延伸音 → swing、
  **一半以上(含一半)**的和弦有六度七度(屬七不算) → latin、其餘 → straight。
  決定 swing 比例、鼓打什麼、貝斯走法、comping 位置
- **曲風可以覆寫,但自動是預設**(`feelPick`:`auto` 或六種曲風的 key)。`feelPick==="auto"` 時 `feelNow` 跟著和弦跑,指定之後就鎖住,
  換和弦也不會被推翻——`C - Am - F - G` 想用搖擺彈(或反過來,拿 ii-V-I 練直的八分)
  是真的需求,自動判得再準也蓋不掉這件事
- **畫面上寫的是曲風名字,不是「直/拉丁/搖擺」**:`Pop` / `City Pop` / `Bossa` /
  `Blues` / `Swing` / `Funk`,
  一律不翻譯。原本寫中文的「直」根本看不出是什麼(八分的流行搖滾?),
  「拉丁」又比實作寬——裡面打的就是 bossa nova 的 clave、框邊小鼓、低音提琴,
  不是騷莎也不是曼波。Swing 與 Funk 在台灣的樂手之間本來就講英文,翻成中文反而要想一下。
  **程式裡的 key 不要跟著改**(`straight` / `latin` / `swing` / `funk` 照舊):
  `GROOVES`、`COMP_CELLS`、`TEMPO_QUICK`、存檔、網址的 `f=` 全都用它,
  改 key 等於把舊的存檔與連結打掉
- 每顆都有 `title` 說清楚那是什麼(`feelStraightTip` 之類),因為一個英文字講不完
  「圓舞曲與 6/8 抒情也走 Pop 這一種」這件事
- **「自動」不是一種曲風,所以不進那排膠囊**。前兩版都栽在這裡:
  先是把它當第五顆膠囊、寫成 `自動 · Pop`,被問「自動旁邊為什麼有 pop?」
  (旁邊就有一顆 `Pop`,同一個字出現兩次);改成「自動亮著、它判到的那顆加外框」
  之後,還是被問「自動我還是不知道是什麼風格」。
  現在的做法是六顆曲風膠囊加旁邊一顆獨立的 `.auto-chip`:
  - **實心金的那一顆永遠是現在在彈的**(`aria-pressed` 比的是 `feelNow`,不是 `feelPick`)。
    看曲風只要看哪一顆是實心的,不用先搞懂「自動」是什麼
  - 自動那顆只回答一件事:「這一顆是跟著和弦挑的嗎」。亮著用**空心金**,
    不跟實心的那顆搶
  - 加曲風的時候不要順手把它塞回那排膠囊裡
- 不要發明新顏色:整套工具的變數只有 gold / ivory / muted / line / danger,
  多開一個色階六個工具都要跟著改
- **那排不要標題**。標過「感覺」(被問「感覺是指什麼」)也標過「曲風」
  (被說「看右邊那幾個我知道是曲風」)——`Pop` / `City Pop` / `Bossa` / `Blues` /
  `Swing` / `Funk` 自己就說明了那是什麼,再加兩個字只是多佔一欄。
  拍號那排要留「拍號」是因為 `4/4` 單獨看不出是在講什麼
- 這幾條取代了原本「感覺畫面上不顯示,不要加標籤」那句:不顯示的前提是不能改,
  一旦能改就一定要看得到現在在彈哪一種
- 門檻是「一半」不是「超過一半」,那是刻意的:`Cmaj7 - A7 - Dm7 - G7`(bossa 的
  I-VI7-ii-V)與 `Fmaj7 - E7 - Am7 - C7`(city pop)都剛好是 2/4,兩個都該是拉丁。
  但「只有一顆」一定不算:`Em - D - C - B7`(安達魯西亞終止)、
  `C - Am - F - G7`(收在 V7 的流行進行)都不該變成 bossa。延伸音則是有一顆就算,
  九度以上很少不小心出現
- **伴奏樣式**(`PATTERNS`,使用者選):pad / comp / drive,只管疏密與力度倍率
- **混合**(`mix`)**不是第四種疏密**,是「每繞一圈換一種」:`MIX_CYCLE` 走
  pad → comp → drive → comp,單位跟力度弧線 `ARC` 一樣是「一圈」(進行的長度),
  所以疏密跟力度是一起起伏的(0.9 / 1.0 / 1.1 / 1.0)。
  換的時機一定落在進行的開頭,不會在樂句中間忽然變密。
  它是由那三種組出來的,所以九種組合一個都沒少,只是會依序走過其中三種
- **排程裡一律問 `styleAt(absBar)`,不要再直接比 `styleOn`**。漏掉的地方混合就不會變,
  而且是「只有混合會怪、其他三種都正常」的那種漏——很難發現。
  `pat()`、`bassMode()`、`compPositions()` 都吃 `absBar` 就是為了這個
- 兩個在排程時交叉(`feel()` × `pat()`)。加新東西時想清楚它屬於哪一軸;
  九種組合每一種都要成立
- `drive` 會把貝斯往上推一級:straight → 八分推進、swing → 走動低音,
  latin 維持 bossa 的形狀(那個切分就是它的味道,改掉就不是 bossa)
- **六種曲風,但只有三種推得出來**。`detectFeel` 只回 straight / latin / swing;
  **City Pop、Blues、Funk 永遠不會被自動選到**,只能自己點或從段落庫點進行帶進來:
  - City Pop 的 maj7 / m7 / 二級屬跟 Bossa 是同一套語彙,差別在律動不在和弦
  - Funk 的 9、13、7♯9 跟爵士同一套語彙,`Em9 - A13` 看和弦分不出是誰
  - Blues 的 `C7 - F7 - G7` 跟「直的 12 小節藍調」一模一樣,差別在八分是不是三連音
  這不是缺陷,是這三種本來就定義在律動上。**不要為了讓它們能被自動判到
  而去猜和弦**(例如「全是屬七就當 Blues」——那樣爵士藍調會被搶走)
- 細分走 `sub`:`FEELS.funk.sub = 2`、`FEELS.citypop.sub = 2` 把一拍切成四格
  (4/4 一小節 16 格)。`subOf()` / `divNow()` 是唯一兩個看它的地方,
  `slotTime()`、`slotsInBar()` 之類的都吃 `divNow()` 所以自動跟著變。
  **加新曲風要細分也走 `sub`,不要另外寫一套格子**
- **City Pop 跟 Funk 差在 hi-hat 走幾分**:Funk 走滿十六分、City Pop 只走八分,
  把十六分留給貝斯與大鼓去咬。乾淨的 8 beat 骨架配十六分的低音律動就是 city pop;
  hi-hat 一走滿就變成放克了
- **Blues 是三連音不是十六分**:`swing: 0.66`(爵士是 0.58,藍調的搖擺更接近正三連音),
  格子跟 Pop 一樣八格,靠 `slotTime()` 把後半格推過去。6/8 就是 12/8 的慢藍調,
  那裡三連音是拍號自己就有的,不再另外搖
- **哪個曲風在哪些拍號成立寫在 `FEELS[x].meters`**,沒寫就是三個都可以
  (Funk、City Pop 只有 `4/4`,Blues 是 `4/4` 與 `6/8`)。`feelFitsMeter()` 是唯一的判斷:
  膠囊變暗、指定被擋、換拍號退回自動全都走它。**不要再針對某一種曲風寫 if**——
  第三個受限的曲風進來時一定會漏掉一邊,而漏掉的症狀是「格子數對不上、整小節沒聲音」
- **鋼琴左手在 boogie 底下要再讓一階**(`lhGain()` 回 0.38 不是 0.55):
  貝斯一小節走八顆(根-五-六-五),左手同時撐一顆長根音,兩層低頻疊在一起會糊。
  藍調鋼琴本來就是左手自己走 boogie 或不碰低音

## 拍號是第三個軸,但它不是使用者要煩惱的東西
和弦決定感覺、使用者決定疏密,**拍號決定骨架**:一小節幾拍、鼓怎麼打、
貝斯的位置、過門在哪一拍。它推不出來(同一組和弦可以是 4/4 也可以是 3/4),
所以一定是使用者選的,但選完就不用再管——三顆膠囊,設定一次。

- `METERS` 只有三個:`4/4`(四拍,一拍兩格)、`3/4`(三拍,一拍兩格)、
  `6/8`(兩大拍,一拍**三格**)。
  **為什麼只有三個**:12/8 的慢藍調跟「4/4 + 搖擺」聽起來是同一件事(差在記譜),
  已經被搖擺那個感覺蓋掉了;2/4 的進行曲寫成 4/4 也一樣。
  5/4、7/8 要自己一整組節奏型才不會是湊數的,先不做
- **所有節奏型一律用「第幾格」寫**,不寫秒數也不寫拍數。一小節幾格 =
  `beats × div`:4/4 八格、3/4 六格、6/8 六格。
  **程式裡不可以再出現寫死的 4**(一小節四拍、count-in 八拍、`musicBeat / 4`),
  一律走 `beatsInBar()` / `slotsInBar()` / `beatSlots()` / `midBeat()`
- `slotTime()` 是唯一把「第幾格」換成時間的地方:單拍子的後半格要搖擺,
  **複拍子不搖**——6/8 的三連音是拍號本來就有的,再搖就變成搖擺的搖擺
- 感覺被拆成兩半:`FEELS` 是跟拍號無關的(搖擺比例、音量、用哪顆鼓、貝斯走法),
  `GROOVES[感覺][拍號]` 才是真的打在哪幾格。**換拍號不是把 4/4 的型剪掉兩格**,
  是換一種打法:圓舞曲的小鼓在二、三,6/8 的小鼓在第二大拍,
  爵士圓舞曲的 ride 是「1 2-da 3-da」,慢藍調的 ride 是 12/8 的 shuffle,
  拉丁的 3/4 與 6/8 共用同一組格子(`[0,2,3,5]`,非洲 6/8 的短鈴型)
- `COMP_CELLS` / `KICK_CELLS` / `SNARE_CELLS` 也都是「感覺 → 拍號 → 一堆型」。
  格子數對不上就整組打不進小節裡,加拍號時三張表都要補
- **每一組現成的進行都帶自己的拍號**(`SECTIONS` 的 `meter`),點下去連拍號一起換。
  同一串和弦在別的拍號當然彈得出來,但「這句歌本來是幾拍子的」是有答案的——
  丸サ進行沒有人用圓舞曲彈、Amazing Grace 不是 4/4。不要讓使用者自己猜
- 點完之後另外兩個拍號**變暗**(`formMeter` + `.dim`)。變暗是提示不是鎖:
  還是點得下去,點下去就當使用者自己要換,提示收起來(`clearFormMeter()`)。
  自己打字、清空、改長度、復原也一律收起來——那時候沒有「本來的拍號」可言
- 接段落時如果拍號跟現在這串不一樣,`formParts` 先清掉當新的一首。
  A 段三拍、B 段六八拍的曲子基本上不存在,硬接只會得到一串要自己清掉的東西
- 播放中換拍號會有一小節接不齊(排程只提前 0.2 秒),下一小節就回到正軌。
  這是刻意不處理的:多數人是按播放前就選好

## 速度跟感覺與拍號綁在一起
- 三顆快捷速度不是固定的 70 / 100 / 120,是 `TEMPO_QUICK[感覺][拍號]` 的慢/中/快。
  爵士圓舞曲本來就比中速搖擺快(♩=130–210)、bossa 在 110–150、
  6/8 的 50 就已經是抒情歌了。和弦一改、拍號一換,這三顆就跟著換
- **不會自己去動使用者打的數字**,只有「單拍子↔複拍子」那一下例外:
  那顆音符從四分變成附點四分,同一個數字不再是同一個速度,
  超出新拍號的建議範圍就換成中間那個值
- 速度前面那顆音符(`bpmNote`)要一起換:6/8 顯示 ♩. ,不然會以為 55 是四分音符

## 段落與形式
- `SECTIONS` 分 4 小節與 8 小節兩組,點一下把段落接到 `formParts` 後面,
  文字框永遠從 `formParts` 重組。使用者自己打字就清空 `formParts` 回自由模式,
  兩邊不能各自為政
- 12 小節藍調自己一區。它全是屬七和弦,`detectFeel` 的 `isDominant()`
  特別把屬七排除在拉丁訊號之外,不然整組會被判成 bossa
- **接段落時曲風不一樣也要當新的一首**(跟拍號同一條規則):A 段 City Pop、
  B 段 Blues 的曲子不存在,硬接的話曲風只會留下後面那一段的,前面那半聽起來就不對。
  `formFeel` 記著「本來是什麼曲風」,跟 `formMeter` 一起在自己打字/清空時歸零

## 節奏要換骨架不是加裝飾
- `COMP_CELLS` / `KICK_CELLS` 每種感覺一組,每小節用 `pickCell()` 抽一個。
  第一個是本家型 45%,其餘平分。不要退回「固定骨架 + 機率裝飾」,
  那樣聽幾小節就鎖定骨架了
- **拍子感的來源不能拿掉,但每種感覺的來源不一樣**:
  流行與放克是小鼓的 2、4 backbeat;bossa 是 clave;爵士是 **hi-hat 踏鈸**踩 2、4。
  即興的人靠那個抓拍子,不管換成哪一種都要一直在
- `arcGain()` 讓力度隨圈數起伏(0.9→1.0→1.1→1.0),一直同一個力度會很悶

## 鼓要照那個曲風真的打法,不是換速度而已
六種曲風的鼓是六種不同的打法,不是同一組鼓打不同的位置:

| | 鈸 | 2、4 | 小鼓 | 大鼓 |
| --- | --- | --- | --- | --- |
| Pop | hi-hat 走八分 | 小鼓 backbeat | backbeat + 幽靈音 | 一、三 + 切分 |
| City Pop | hi-hat 走**八分** | 小鼓 backbeat | backbeat + 十六分幽靈音 | 十六分切分,比放克少 |
| Bossa | hi-hat 走八分 | clave | cross stick 打 clave | surdo 一、三,很輕 |
| Blues | hi-hat 走八分,**是三連音的** | 小鼓 backbeat | backbeat + 幽靈音 | 一、三 |
| Swing | **ride** 打 1・2・2的後半・3・4・4的後半 | **hi-hat 踏鈸** | 稀疏 comping | feathering 四分,很輕 |
| Funk | hi-hat 走**十六分** | 小鼓 backbeat | backbeat + 十六分幽靈音 | 十六分切分,不落在正拍 |

- **爵士的 ride 不是走滿八分**,是那個「ding ding-da ding」的型(`cymPattern:[0,2,3,4,6,7]`),
  後半拍那兩顆(skip note)要輕。走滿八分就變成搖滾的 hi-hat 加了搖擺,聽起來很怪
- **爵士的小鼓不是 backbeat**。打 2、4 是搖滾/shuffle 的打法;爵士的小鼓在做 comping,
  用 `SNARE_CELLS.swing` **平均抽**(`pickCellEven`,沒有本家型)——
  連三小節打同一個位置就變成節奏型,那就不是 comping 了
- **爵士的大鼓是 feathering**:四分音符、幾乎聽不見(0.16),`KICK_CELLS.swing` 抽到的
  那顆才是重音(丟炸彈)。推進不加「二的後半」那顆推力,那是搖滾的推法
- **bossa 的 clave 是兩小節一循環**:3 邊 `[0,3,6]` 與 2 邊 `[2,5]` 輪流。
  每小節都打同一邊就只是一個節奏型,不是 clave。而且要用 cross stick(框邊敲擊),
  不是一般的小鼓中心擊——bossa 從頭到尾沒有人在打小鼓中心
- **推進的推力也要照感覺換位置**:流行搖滾推「二的後半」、bossa 推「四的後半」
  (進下一小節的預備),爵士不推
- **搖擺的過門走三連音**(`spb/3`)不是十六分。八分是搖擺的、過門突然變成整齊的
  十六分,那一拍會整個掉出來
- **放克的 hi-hat 要有強弱**,不然十六分會變成打字機:正拍 1、後半拍 0.78、
  十六分那兩格 0.5(`div === 4` 時照 `k` 給),那個強弱差就是 funk 的律動來源。
  大鼓與貝斯的格子刻意都不落在第二、第四拍的正拍上(`[0,3,10]`、`[0,6,10,14]`),
  空出來的地方才是 backbeat 跟切分聽得出來的原因
- 鼓件依感覺載:`FEEL_PIECES` 說這個感覺要哪幾顆,`ensureDrums()` 會等它們
  (ride 換成合成音就不是爵士了)。ride 兩顆 round-robin 共 2MB,
  只有搖擺的進行才會下載。放克用的是基本三件(`FEEL_PIECES.funk` 是空的),
  不會多載任何東西

## 現成的和弦進行
- **全部維持 C 大調 / A 小調,不要放別的調**。這是拿來練的,調性固定才好上手;
  要換調有移調滑桿。曾經散到 F / Em / G / Bb,被打回來過
- **每一組都要是真的常用的完整進行,不要湊數**。四小節那組要「自己循環得起來」——
  繞回第一小節要接得順;八小節那組要是完整的一句,最後一顆接得回開頭。
  `F - G - Em - Am` 就是被退掉的例子:它是王道進行(4536251)的前半,
  單獨當四小節循環並不完整,那一整句要放在八小節那組
- **每一組進行都帶自己的曲風(`feel`)與拍號(`meter`)**,點下去兩個一起換。
  曲風跟拍號一樣是推不出來的:丸サ進行是 City Pop 不是 Bossa、`C7 - F7` 是 Blues
  不是「直的屬七」,和弦看不出差別。所以 `SECTIONS` 的每一筆都要填 `feel`,
  漏填的那組點下去曲風不會換,而且**不會報錯**——很難發現
- 目前 54 組,按曲風分頁:Pop 20、City Pop 8、Bossa 6、Blues 5、Swing 10、Funk 5。
  同一個曲風裡再按「長度 · 拍號」分區
- **哪個曲風配哪些拍號是有答案的**:圓舞曲與 6/8 抒情都是 Pop(換的是拍號不是曲風)、
  City Pop 與 Funk 只有 4/4、Blues 只有 4/4 與 6/8(12/8 慢藍調)、
  爵士圓舞曲是 Swing 的 3/4。不要為了湊滿 6×3 去補不存在的組合
- **爵士的 3/4 與 6/8 要用九度以上的聲位**,不然 `detectFeel` 判不到搖擺:
  `Cmaj7 - Am7 - Dm7 - G7` 四顆裡有三顆是六七度,會被判成拉丁(afro waltz),
  寫成 `Cmaj9 - Am9 - Dm9 - G13` 才是爵士圓舞曲。慢藍調同理:
  `C7 - F7 …` 是直的 6/8、`C9 - F9 …` 才會走 12/8 的 shuffle ride
- 膠囊上只顯示和弦,不要寫風格名字(註解裡可以寫,那是給改程式的人看的)
- **循環進行不要硬撐成八小節**。丸サ進行(`Fmaj7-E7-Am7-C7`)最後那顆 C7 是
  「把 Fmaj7 當暫時主和弦」的屬七,`Vm7-I7-IVM7` 等同 ii-V-I,所以它天生就是
  四小節一直繞。曾經放過一組「第八小節停在 Am7」的八小節版,那不是真的曲子的寫法,
  已經退掉;要八小節按 ×2 就好。八小節那格改放王道的七和弦聲位
  (`Fmaj7-G7-Em7-Am7-Dm7-G7-Cmaj7-C7`),那才是真的八小節一句

## 編輯流程:先定長度,再一格一格改
使用者要的流程是「先選 4 / 8 / 12 小節 → 點第 1 格改它 → 點第 2 格改它」,
不是「點音名就往後長一格」。所以:

- **`lenSeg`(4 / 8 / 12 小節)是進行的長度,不是段落庫的分類**。按下去把進行
  變成 N 格:變多就把現有的重複貼滿(4 → 8 就是同一句唱兩次)、變少就截掉,
  然後停在第 1 格。亮的那顆是「現在幾小節」
- **一直有一格在編輯**。點格子換一格,點同一格不會取消(取消了鍵盤就不知道
  要改誰);沒選到時 `targetBar()` 當第 1 格
- **點音名/性質只改正在編輯的那一格,不會自己往後跳**。改完第 3 格還是停在
  第 3 格。一格都沒有的時候才開一格出來
- 鍵盤:`←` `→` 換上一格/下一格,數字鍵 `1`–`9` 直接跳到那一格
  (焦點在輸入框裡時完全不攔)
- 段落庫**不能**再放一個「4 小節 / 8 小節」的切換:那個標籤跟上面的「小節數」
  撞在一起(一個改進行長度、一個換清單),一定會按錯。清單改成兩組一起列,
  中間插一條小標
- **段落庫的分頁是曲風,不是拍號**。使用者想的是「我要練 City Pop」,
  不是「我要練 4/4」——拍號是進行自己帶的,點下去就換好了,不必先選。
  分頁的名字跟右欄那排曲風**一模一樣**(Pop / City Pop / Bossa / Blues / Swing / Funk),
  兩邊對得起來才看得出「這一頁就是那一種」

## 編輯就在小節格旁邊
和弦進行有三個地方可以改:文字框、和弦鍵盤、小節格。它們動到的是同一串文字
(`barTokens()` / `setTokens()`),所以要擺在一起,不要散在兩欄的兩端。

- 左欄的順序是 **輸入 → 小節數/段落庫 → 小節格 → 播放 → 和弦鍵盤(含小節工具列)**;
  右欄只放伴奏設定(樣式、速度移調、開關、存檔)。
  和弦(小節格)跟播放鈕在上面、選字母與性質的鍵盤在下面:上半是「這首曲子長怎樣、
  按下去聽」,下半是「要改就在這裡改」。播放鈕從右欄搬到左欄就是為了這個順序,
  手機疊成一欄時也是同一串
- 走帶搬進左欄等於把 58px 加在本來就比較長的那一欄,720 高的筆電放 12 格、32 格
  就爆了。補回來的方法寫在下面「版面」那節(clamp 的係數要讓 720 取到下限、
  兩欄螢幕的小節格再矮一階),不要靠把播放鈕搬回右欄解決
- 小節工具列併在鍵盤的底列(`.pal-foot`),不要自己佔一排:那一列本來就有
  「正在改第幾格」的提示,而且獨立一排會多 35px。按鈕一直都在、沒選取時停用,
  不然一選取就多一排、整個版面會跳
- 工具列**只有三顆**:插一格(複製正在編輯的那一格)、刪一格、復原。
  曾經還有 `◀ ▶` 搬移,砍掉了:四小節的練習循環很少需要換順序,
  真要換順序在文字框裡改比較快,而五顆圖示鈕擠在一排反而看不懂。
  加按鈕之前先問「這件事沒有它就做不到嗎」
- **復原**存的是整串文字的前一版(`pushUndo()`),鍵盤、段落庫、工具列、清空、
  改長度都要先呼叫它。文字框自己打字不用經過(瀏覽器有內建的復原),
  ⌘Z / Ctrl+Z 在焦點不在輸入框時才攔

## 空出來的版面拿來放內容,不是拿來放大按鈕
平板上卡片浮在中間、上下各空 200-300px,被打回來兩次。兩條路都試過了:

1. **把按鈕放大**(--grow 開很大)→ 按鈕變成怪物,而且還是空
2. **把卡片撐滿、縫拉開**(`min-height:100vh` + `margin-top:auto`)→
   一個接縫被拉開將近 200px,版面整個散掉

對的做法是 **3. 多出來的高度給「會長的東西」**:段落庫是清單,多高都合理。
螢幕夠大(`min-width:720px` 且 `min-height:900px`)時:

- `.card` 給**定高** `calc(100vh - 148px)`、`grid-template-rows:auto 1fr auto`,
  段落庫那一列是 `1fr`。給定高不是 min-height:格線的 `1fr` 要有確定的高度
  才收得住,不然清單會照自然高度長出去、整頁被撐破
- `placeSecPanel()` 把 `#exPanel` 從左欄的浮動位置搬到卡片底下的 `#exInline`
  (整排),`body.sec-inline` 帶動樣式;視窗變化時再搬回來
- 這樣 1366 寬的平板橫放一次看得到 17 組進行、13 吋直放看得到全部 24 組,
  上下留白只剩 20px
- `--grow` 同時**調小**(`clamp(0px, calc(2.2vh - 16.1px), 16px)`):
  元件只長一點點,空間留給清單
- 門檻不能再低:720×900 已經是極限(32 小節時清單只剩 49px),
  680 寬或 820 高再放內嵌清單,12 小節就會把整頁撐破(實測 +13px)
- `--grow` 動過就要重量 **4/8/12/16/24/32 小節 × 14 種尺寸**,一個都不能漏
- 速度與移調那兩塊要用 **container query**(`container:ctl / inline-size`)
  擋住圓鈕長大:平板直立時一塊只有 156px 寬,44px 的圓鈕會把滑桿擠成一個點、
  刻度 −12 / 0 / +12 疊在一起。條件寫 `.control .step-btn`(兩層)才蓋得過
  後面 `@media` 裡的 `.step-btn`
- 不要改 `.control-pair` 的 `minmax(152px,1fr)` 來解這件事:改成 168px 的話
  手機(卡片寬 330px)會掉成一欄,整頁高度多 128px

## 段落庫是浮動面板,預設收起來
- 13 + 11 組清單展開有 200px 出頭,一直佔著左欄快一半的版面——多數時候
  使用者只是要看自己那一段,所以預設收起來,開關上寫著有幾組
- 面板 `position:absolute` 浮在卡片上,**不要把下面的東西推開**:一推開整頁就爆掉
  (實測展開會超出 150px 以上),而且看完就收起來了,本來就不該佔位置
- 清單高度上限由 `fitSecPanel()` 在打開時量:「從清單上緣到視窗底還有多少」。
  純 CSS 的 `min(52vh, 430px)` 不夠——面板雖然是浮的,但比視窗剩下的高度長的話
  整頁還是會被撐出捲軸(24 組一起列之後踩到)
- 清單的結構是**曲風分頁在最上面、裡面再按「長度 · 拍號」分區**:
  六顆曲風的 `seg-mini` 放在面板頂端(`#secFeelSeg`),下面依序是
  「4 小節」「8 小節」「12 小節」,同一個長度裡再按拍號拆開。
  長度分區是因為「我要練四小節的循環」跟「我要練一整句」是兩件事,
  混在一起找不到(36 組一路排下來被退過)
- **標題只在那個曲風橫跨兩個以上拍號時才寫拍號**(`secLenMeterGroup`)。
  Bossa 全是 4/4,寫「4 小節 ・ 4/4」是廢話;Pop 的 3/4 是圓舞曲、6/8 是抒情歌,
  那是兩種不同的歌,不寫的話點下去才發現拍號變了
- 分頁**只換清單**,不會動到現在這首的曲風與拍號;真的要換是點下面的進行
- **曲風真的變了才把清單跳頁**(`updateFeel` 裡比的是「跟剛才一樣嗎」,
  不是「跟分頁一樣嗎」)。比分頁的話,使用者切到別的分頁看,打一個字就被拉回來
- 內嵌的時候一排排得下四五組(`minmax(176px,1fr)`),八小節的佔兩格、
  12 小節的佔三格(`.long` / `.xlong`)——整排 1000px 只放一組太浪費
- 正在用的那組會亮起來(`.ex-btn.on`,比對 `formParts`):清單一直開著的時候
  要看得出來自己在練哪一個
- `.examples` 的 `overflow-y:auto` **不要放在 `@media (min-width:680px)` 裡**。
  手機沒有那條規則的話,36 組會整串攤開把頁面拉長一千四百多 px(踩過)
- 點面板外面或按 Esc 收起來;點面板裡面(挑段落)不關,才接得出 A A B B。
  整個 `.ex-wrap` 都算「裡面」,不然按「清空」想重接一次形式時面板會跟著關掉

## 移調時畫面要顯示真的在響的和弦
- 文字框留使用者自己打的那一版不動(那是原稿),小節格與走帶列顯示移調後的名字
  (`chordDisplay()`):`C - F - G - C` 移 +2 就顯示 `D - G - A - D`
- **拼法要照樂理走,不能拿「pc → 固定音名表」硬換**。+1 是「上小二度」,字母要往上
  走一格:C 變 `D♭` 不是 `C♯`。照表換的話 `Dm7-G7-Cmaj7-Fmaj7` 會變成
  `E♭m7 - A♭7 - C♯maj7 - F♯maj7`——同一段裡升降混用、四度的字母鏈也斷了,
  正確的是 `E♭m7 - A♭7 - D♭maj7 - G♭maj7`(D♭ 大調)。被抓過一次
- 走幾格字母是**整段一起決定**的(`transposeSteps()`),不是一顆一顆各自算,
  不然同一段裡會出現 `C♯maj7` 跟 `E♭m7` 並排。本位走法之外也試同音異名的兩個,
  用「有沒有重升重降 / 有沒有 C♭ F♭ B♯ E♯」計分挑最順的那個:
  `Bb - Eb - F - Bb` 上小二度照算是 `C♭ - F♭ - G♭ - C♭`,計分之後會寫成 B 大調
- `parseChord` 除了 pc 還要留原本寫的**字母**(`rootLetter` / `bassLetter`)、
  性質(`suffix`)與有沒有轉位低音(`slash`):只有 pc 拼不出正確的音名,
  也組不回完整的和弦名
- 性質裡的升降也排成正式記號(`prettySuffix`):`m7b5` → `m7♭5`、`7#11` → `7♯11`。
  只換「記號後面接數字」的,`add9` / `sus4` / `alt` / `It+6` 都不會被動到。
  根音已經是 ♭ ♯ 了,性質還留 ASCII 的 b 會很難看
- 小節數那一列會補一句「下面是移調 +2 的和弦」。不講的話會以為文字框沒跟著改是壞掉了
- 標題右邊那一行(`updateBarCount()`)一次把**形式、小節數、移調**講完,不要再另外開一行。
  曾經有一條 `form-line` 寫「自由輸入 ・ 12 小節」,跟上面的小節數是同一句話,
  還多佔 22px
- `setTranspose()` 要重畫小節格。播放中重畫沒關係:下一幀 `paintPlayhead`
  會把正在響的那一格重新點亮

## 練習用的小東西
- **愈練愈快**(`rampOn`):每繞兩圈 +5 BPM,到 240 為止。在「圈的第一小節第一拍」
  走 `setBpm()`(也就是 `reanchorTempo`),已經排進去的音不會被改到一半。
  `rampedCycle` 記住加過的那一圈,同一圈不會加兩次;按播放時歸零
- **複製連結**(`shareURL()` / `applyURLParams()`):和弦、速度、拍號、樣式、移調
  編成 `?c=…&bpm=…&m=…&s=…&t=…`。老師把一條網址傳給學生,對方打開就是同一題。
  **網址有參數就蓋過 localStorage**——傳連結的人要的就是「對方看到的跟我一樣」
- 加到主畫面會用全螢幕開(`apple-mobile-web-app-capable`),平板架在譜架上不會被
  網址列吃掉一條。**沒有 service worker**:那要另外一個檔案(單檔 HTML 的規則),
  而且取樣在 CDN 上,真要離線得連取樣一起快取,是另一件事

## 被提過但**刻意不做**的
別人看到這個工具常會建議下面幾件,每一件都想過了:

- **當作入口的曲風選單**(打開就要先挑 Pop / Swing / Bossa / Ballad):做過,被打回來過。
  現在的「感覺」那排是**覆寫**不是入口——預設那顆是自動,自動判出來的才是常態,
  九成的人一顆都不用按。不要把它改回「使用者一定要先選一種」,也不要把自動拿掉
  (詳見「兩個軸」那節)
- **各軌靜音 / 混音器**(「貝斯手想把貝斯拿掉」):跟下面「鼓與貝斯永遠都在」直接衝突。
  那個需求是真的,但它等於要一個 minus-one 模式,不是加一顆靜音鈕就好——
  要做要先講清楚是不是要改掉「永遠都在」這條
- **MIDI / 音訊匯出**:MIDI 寫得出來(單檔、無相依),但這個工具的定位是「打開就跟著彈」,
  匯出是 DAW 的事。真的要做再說
- **小節區段循環**(只循環第 3–4 小節):有用,但要在小節格上加「選範圍」的互動,
  目前的小節格是「點一格編輯那一格」,兩種選取會打架。想清楚再做

## 鼓與貝斯永遠都在
沒有開關，不要再加回去。`lhGain()` 固定回 0.55（貝斯一直在，鋼琴左手要讓位）。

## 響度
拿來跟著彈的，音量不夠會被真鋼琴蓋過去。鏈路是
`masterGain ×1.9 → 限幅器(−10dB, 20:1) → outGain ×1.25`。
三段一起調，動其中一個就要重新量：目標 peak ≤0.85、RMS −12～−14 dBFS、零削波。
outGain 放到 2.4 會讓尖峰衝到 1.6 直接爆掉（實測過）。

## 版面
- 680px 以上分兩欄（左 `.col` 輸入、右 `.col` 播放），目標是整頁塞進一個畫面。
  改版面時拿 `document.body.scrollHeight` 量：1280×720 / 1280×800 / 1440×900 /
  1100×800 / 900×800 / 800×800 / 820×900 都要 ≤ 視窗高，而且 4、12、24、32 小節的
  進行都要一樣高（小節多就靠 `.bars-grid` 的 dense / denser 換小格）。
  平板與桌機量過的尺寸（720–1366 高、744–2560 寬）整頁都不用捲，
  段落清單放不下時是清單自己捲、不是整頁捲。只有手機是已知要捲的
- 斷點用 900 / 680 / 600 / 400，跟 ScrollScore 與首頁對齊，不要自己另訂
- 窄螢幕疊回一欄，兩欄的 DOM 順序必須跟單欄想要的順序一致
- 一排按鈕數量不固定時用 grid 不要用 flex：flex 會把最後一行剩下的幾顆撐滿整列，
  同一顆鈕在不同行寬度差一倍（音名列、開關列、範例列都踩過）。
  用 `auto-fill` 不要 `auto-fit`——`auto-fit` 會把空格子收掉，最後一行又被撐開
- 目前每一塊都是等寬格線：範例 2 欄、音名 6 欄、性質 7 欄（手機 5 欄）、小節格 4 欄。
  加東西進去要跟著對齊，不要讓某一塊變成參差不齊的 flex
- 範例膠囊裡面也是格線：一顆和弦一個等寬格子、置中，跟「一個和弦一小節」對得上。
  四小節的段落一排兩個，八小節與 12 小節藍調一組佔一整排（`.ex-btn.long`），
  不要讓 8 顆和弦跟隔壁那組擠在同一排——看不清楚也容易點錯
- 膠囊一律有邊框加底色（`--surface-inset`），每一組的範圍要一眼看得出來；
  手機 `min-height:34px`，那是用手指點的
- **小節格一排四格**，因為樂句就是四小節一句：第 5 格一定在第二排的開頭，
  12 小節藍調剛好三排。超過 12 格才改成一排八格（等於一排兩句），第 4 與第 5 格
  之間開一點縫（`.bars-grid.eight .bar-chip:nth-child(8n+5)`）把兩句分開。
  **不要用六欄或兩欄**：六欄會讓第 5 格跑到第一排中間，看的人就數不出樂句了
- 右欄用 `justify-content:space-between` 把多出來的高度分掉。左欄（輸入＋編輯＋播放）
  本來就比右欄長，不分的話右下角會空一塊
- **曲風那排貼在拍號下面**(`.feel-row`,`.seg-mini` + 旁邊的 `.auto-chip`),
  不要放進伴奏樣式那塊:
  伴奏樣式是「疏密」、曲風是「哪一種律動」,兩個是不同的軸,擺在一起會以為要二選一。
  右欄由上而下是「伴奏樣式 → 拍號 → 曲風 → 速度移調 → 開關 → 存檔」,
  拍號與曲風兩排小膠囊挨著,因為它們都是「設定一次就不用再動」的東西。
  四顆曲風加一顆自動在手機 333px 寬排得下一排,`flex-wrap` 是留給更窄的情況
- **拍號放右欄、貼著速度**,不要放左欄「小節數」那一排:那一排是
  `[4 小節][8 小節][12 小節]`,旁邊再放一個 `[4/4][3/4][6/8]` 一定會按錯
  (一個改進行長度、一個改一小節幾拍,長得又像)。
  而且拍號跟速度要一起讀——6/8 的 60 是附點四分音符的 60。
  左欄也早就沒有空間了(見「編輯就在小節格旁邊」那節的 58px)
- 拍號用 `.seg-mini`(小膠囊)不用 `.seg`(大按鈕):設定一次就好的東西
  不需要跟伴奏樣式搶注意力,而且大按鈕一排會多 51px
- **clamp 的 vh 係數要讓 720 高的螢幕剛好取到下限**(`Nvh × 720 ≤ 下限`)。
  寫 `clamp(4px,1vh,16px)` 的話 720 高會拿到 7.2px、比下限多 3px,一個元件三、四 px,
  整欄加起來就是 20px,剛好是 1280×720 放不放得下 12 格的差別。
  要撐高螢幕是靠上限,不是靠係數
- 兩欄的螢幕(`min-width:680px`)小節格可以再矮一階:`dense` 24px、`denser` 22px、
  間距 6px。手機維持 28 / 24 與 8px——那是用手指點的,不能跟著縮
- **高螢幕不要讓卡片浮在上面、下面空一大塊**(13 吋平板直立 1024×1366 踩過)。
  兩件事一起做:`@media (min-width:680px)` 裡讓元件與區塊間距跟著視窗高長大
  (`clamp(原尺寸, Nvh, 上限)`——短螢幕取到下限,完全不受影響),
  剩下的空間再用 `.stage{margin-block:auto}` 平均分到上下。
  用 margin auto 不要用 `justify-content:center`:空間不夠時後者會把上緣頂出捲動範圍
- 段落清單在浮動面板裡,高度上限 `min(52vh, 430px)`,跟其他版面無關。
  (曾經用 `calc(100vh - 607px)` 把它跟整頁高度綁在一起,搬過版面就要重量一次,
  改成浮動面板之後這條就不需要了)
- 清單捲得動的時候上下要有漸層(`.more-above` / `.more-below`,JS 依捲動位置掛),
  不然被切一半的那一排看起來像畫壞了。捲軸的 thumb 用 `--muted`,
  用 `--line` 在深色卡片上根本看不見
- `auto-fill` 的 `minmax()` 下限訂太大會在中間寬度掉成一欄，頁面一下子長一截：
  900–800px 那一段欄寬只剩 340–400px，範例是 `minmax(164px,1fr)`、
  速度移調 `.control-pair` 是 `minmax(152px,1fr)` 才留得住兩欄
  （原本 188 / 196，820px 時範例變 8 列、900px 時速度移調疊成兩列）。
  而且這裡會自己咬自己：一掉成一欄頁面就變高 → 冒出捲軸 → 欄又窄 15px →
  更掉不回去，所以量的時候要看有捲軸的寬度，不要拿理論值算

## 技術棧
- 單檔 HTML，CSS/JS 內嵌，CDN only，無 build，GitHub Pages 直接部署
- 配色/字體/元件樣式跟 ScrollScore、SightScore、LoudMaster 共用同一套變數，不要自己發明新的
- 一個和弦一小節；拍號 4/4 / 3/4 / 6/8 三選一（見下面「拍號」那節）

## 絕不能做的事
- 不要把單檔拆成多檔案，除非我明確要求
- 不要改用 setTimeout 排音符。排程一律走 `AudioContext.currentTime` 提前排，
  setInterval 只負責「每隔一陣子來看一下」，不決定任何一個音的實際時間點
- 不要把取樣改成一次載完整組；iPad 第一次開會等很久。只抓這次進行真的會用到的音
- 不要拿掉解析失敗時擋住播放的那一段。看不懂的和弦要明講是哪一個 token，
  不要自作聰明猜成別的和弦

## 已知踩過的坑
- SVGElement 沒有 hidden 這個 IDL 屬性，`el.hidden = true` 只會掛上一個沒人看的 JS 屬性，
  內容屬性不會變。播放/停止圖示一定要走 setAttribute / removeAttribute（見 `hideSvg`）；
  而且瀏覽器內建的 `[hidden]{display:none}` 綁在 HTML 命名空間上，對 `<svg>` 無效，
  要自己寫一條 `svg[hidden]{display:none}`
- 速度改變時不能直接拿舊的 anchorTime 來減，要重新錨定：讓「下一個還沒排的拍」維持原本
  該響的時間，新速度從那一拍之後才生效，已經排進去的音不會被改到一半（見 `reanchorTempo`）
- 正規化時不要動 `+`。寫 `/+/g` 是「Nothing to repeat」語法錯誤（整支 script 都不會跑），
  就算改成 `/[+]/g` 寫對了，`It+6` 也會被換成 `Itaug6`、`7+5` 變 `7aug5`，兩個都查不到。
  `+`、`7+5`、`It+6` 一律當字面 key 收在 QUALITY 表裡
- `/` 有兩個意思：`C/E` 是轉位低音，`6/9` 是和弦記號自己帶的斜線。只有斜線後面
  真的是音名時才當轉位，否則整串留著查表（用 lastIndexOf，`C6/9/E` 才切得對）
- 中文輸入法很容易打出 `Ｃ７` 這種全形字，正規化第一步就要把全形英數符號轉半形
- 六、七個音的和弦不要整組照彈。捨音順序：五音 → 根音（轉位時不能捨）→ 延伸音，
  上聲部最多四個音（見 `trimTones`）
- 和弦品質表的 key 大小寫有意義（`M7` 是大七、`m7` 是小七），不能整串 toLowerCase
- 每小節都要從所有轉位裡挑離前一小節最近的那個，不然 C→F→G 會整組跳上跳下，
  聽起來像機器在按琴鍵（見 `buildVoicing` 的 cost）
- Salamander 每個八度只錄 C / D# / F# / A 四個音，其餘用 playbackRate 補位。
  音域是 A0–C8，鋼琴全音域都蓋得到，低音區不需要另外處理
- 鼓是 FLAC，`decodeAudioData` 對 FLAC 的支援不如 mp3（舊的 iOS Safari 可能吃不下），
  所以每個鼓件都要留合成備援（見 `synthDrum`），不能假設取樣一定載得到
- 貝斯一律待在 E1–D#2（`bassMidiFor`）。移調只換音級、不跟著跑到別的音域，
  不然移幾度貝斯就飛到鋼琴中間、低音整個空掉
- 貝斯軌開著時鋼琴左手要降音量（`lhGain`），同一個根音疊兩層低頻會糊
- 三種伴奏樣式的左手低音都要留長（撐兩拍，墊底撐整小節），不要只響一個音就收掉
- 變化用 `hash01(絕對小節號, salt)`，不要改成 `Math.random()`：那樣連「這一拍到底
  加了沒」都無法重現，出問題沒辦法追
- `hash01` 一定要兩輪 murmur 收尾。第一版只混一輪，整體分布看起來沒問題
  （四分位都是 0.25），但把 salt 固定、小節號從 0 數上去會連出一串很小的值，
  實測貝斯前五小節全部選到同一條路。雪崩不夠就是會這樣，不要為了短而簡化它
- 過門的鼓件（htom / ltom / crash）加起來 2MB 出頭，要背景載、不要擋播放
  （見 `ensureDrums` 裡沒有 await 的那條）。還沒到位時 `synthDrum` 會頂著
- 一格的進行沒有「最後一格」可言，過門只在兩格以上才給
- 大段落搬移程式碼時，先確認 `hash01` / `chance` 這種被大家用到的小工具沒被一起搬走。
  整段換掉 `STYLE_FNS` 那次就把它們刪掉了，症狀是「取樣都載好了但按播放沒聲音」，
  而且 pageerror 抓不到（錯誤發生在 promise 的 .then 裡），要靠 window error 才看得到
- 和弦名字被切掉比字小一號嚴重得多：`Cmaj7` 切成 `Cm…`、斷行成「Cmaj / 7」都會被
  看成別的和弦。格子不夠寬時一律縮字（範例膠囊、小節格、性質鈕都是同一招：
  `font-size:min(原字級, calc(格子寬 ÷ 字數 ÷ 常數))`），常數是量出來的
  （Noto Serif 一個字約 0.73 個字高，取 0.76 留餘裕）。算的時候要扣掉自己的內距與
  邊框，`100cqi` 給的是內容寬、不含 padding，少扣一次就會剛好差幾 px 又被切到
- `@container` 的條件是對「最近的祖先容器」算的，不是寫在哪一塊就算哪一塊。
  `.examples` 與 `.ex-btn` 兩層都是容器時，`@container (max-width:340px){ .ex-ch{…} }`
  會拿 `.ex-btn` 的寬度去比，不是 `.examples` 的——踩過一次，症狀是 800px 寬時
  字級沒縮、和弦被切掉。所以容器一律取名（`exbox` / `exrow` / `palq`），
  條件也一律寫成 `@container exbox (…)`
- 同一排的字級要一致：範例膠囊的 `--len` 取「同樣長度的進行裡最長的和弦字數」，
  不要每一組各算各的，不然同一排字忽大忽小
- container query 是 2022 年以後的瀏覽器才有。老瀏覽器上 `100cqi` 整條 font-size
  會被丟掉，退回原本的字級（窄的時候會切字），這是可以接受的退化——不要為了它
  改回 media query，這一塊在單欄與兩欄的寬度差很多，看視窗寬度一定判錯
- 過門不要佔滿最後兩拍、也不要寫成整串 tom 滾奏：從八分突然跳成十六分很突兀。
  預設只佔最後一拍，hi-hat 要繼續走不能斷，力度從很輕推上去
