# BackingTrack 專案規則

## 和弦詞彙
- `QUALITY` 表是對著 HarmonyMap 的和弦資料庫生出來的：它的 80 種和弦類型、
  連同每種的別名寫法，共 134 種拼法。HarmonyMap 加了新和弦，這裡要跟著補，
  兩個工具看得懂的東西必須是同一套
- 兩個不收：「So What」有空白當不了 token；「♯5」跟 `C♯5` 撞在一起，一律當後者
- `normalizeSymbol()` 已經吃得下爵士表上的寫法：`∆ Δ △` → maj、`° ◦` → dim、
  `−`(U+2212) → m，全形英數也先轉半形。`min7` / `M7` / `ø` / `o7` 本來就在表裡
- **半形的 `-` 是特例**：它同時是小節分隔符（`C - Am - F - G`），所以只在
  「根音後面緊接著 `-`，再接數字或結尾」時才當小三和弦（`C-7` → `Cm7`、`Bb-` → `Bbm`）。
  `C-Am`（後面接音名）跟 `C - Am`（有空白）還是分隔符。這條在 `parseProgression`
  切 token **之前**做，切完就來不及了
- 轉位（slash chord）本來就完整支援：`C/E`、`Am7/G`、`C6/9/E` 都吃得下，
  貝斯與鋼琴左手彈的是斜線後面那個音（`chord.bassPc`），不是根音

## 音源
三軌都在 sfzinstruments 這個 org 底下（跟鋼琴同一家），要換之前先問：
- 鋼琴：Salamander Grand，跟 ScrollScore 指向同一組檔案
- 鼓：Virtuosity Drums（Versilian Studios × Karoryfer，KVRDC'21）。
  這是波士頓實錄的**爵士鼓組**（鼓手 Austin McMahon），不是管弦打擊樂。
  取 `Samples/mid/`（中距離麥克風）那一組
- 貝斯**兩把,照感覺換**(`BASS_KITS` / `bassKit()`):
  - 直(流行搖滾)→ Black And Blue Basses 的 `darkblack` 指彈電貝斯,mf 力度
  - 拉丁與搖擺 → D. Smolken 的 1958 Otto Rubner **低音提琴**撥弦(`dsmolken.double-bass`
    的 `pizz/`,m 力度、round robin 取 a)。爵士的走動低音本來就是低音提琴,
    bossa 也是(Getz/Gilberto 那個年代就是尼龍弦吉他加低音提琴)
  - 兩把都是 D. Smolken 錄的,音色調性一致;**檔名的音高寫法不一樣**:
    電貝斯是 `midi = 八度 × 12 + 音級`(`e2` = 28),低音提琴是科學音名
    (`pizz_g1` = 31,sfz 裡的 `pitch_keycenter` 就是這個數字)。不要混用
  - buffer 分兩個 Map 收(`bassBuffers.electric` / `.upright`),
    `playBassNote` 只從現在這把裡挑最近的音——混在一起會抓到另一把的取樣

- 電鋼琴:**jlearman.jRhodes3c**(1977 Rhodes Mark I Stage 73),一樣在 sfzinstruments 底下。
  City Pop / Funk / K-Pop 用它,其餘五種用 Salamander(`FEELS[x].piano`,跟貝斯的 `kit` 同一個做法)
  - **取 mono 那一組**:單聲道一顆 75–175KB,立體聲是兩倍大;立體聲寬度由 `playNote`
    自己的 panner 給,不必付兩倍頻寬
  - 檔名是 `As_<midi>__<音名>_<力度層>-mono.flac`。力度層那個數字**每往上一個取樣點 +2**
    (F1 是 279、B1 是 281、E2 是 283…),但**不是每一層都錄滿整個鍵盤**。
    所以 `RHODES_TABLE` 是兩層拼的:F1–E5 走 279 那層、A5 以上那四顆只剩 453 那層。
    **不要照公式去推 279+2n 的高音**——A5 的 301 不存在,會整顆 404
  - 兩層的峰值一樣(量過都在 0.64–0.75),接縫只差亮度不差音量,而且那四顆在最上面
    一個八度,伴奏聲位幾乎踩不到
  - **`PIANO_KITS.rhodes.gain` 是 0.62,要往下收不是往上加**:同一個 peak 參數下
    這組取樣比 Salamander 大一倍(四個音的和弦 2.37 vs 1.17)。動它就要重量整組響度
  - Rhodes 是 FLAC,舊 iOS Safari 可能整組吃不下。**整組都掛時退回平台鋼琴**
    (`ensureSamples` 裡那一條),不是掉進三角波的保底音色——
    電鋼琴變鋼琴只是音色不對,變合成音會整首不能聽
- **808 是合成的,不是取樣**(`synth808` / `E808_TRIM`,鼓;`BASS_KITS.sub808`,貝斯)。
  TR-808 本來就是一台合成鼓機,所以這一組**一個檔案都不下載**,
  而且 `ensureDrums()` 在 `drumKit() === "e808"` 時直接回傳——不要順手把爵士鼓組也載下來
  - `synthDrum` 與 `synth808` 是**兩件事**:前者是「取樣載不到時的備援」(要盡量像那顆鼓),
    後者是「指定的音色」。不要把 808 寫進 synthDrum 的分支裡
  - **合成音一定要先做電平校正**(`E808_TRIM`)。同一個 gain 下 808 大鼓比實錄大鼓大十二倍、
    疊方波的 hi-hat 是四十倍(量過 gain=1 的 peak:取樣大鼓 0.084 / 808 大鼓 1.065、
    取樣 hi-hat 0.022 / 808 hi-hat 0.917)。沒有這張表 `FEELS[x].kickGain` 對兩套鼓
    就不是同一個意思,K-Pop 一按下去就削波(踩過:peak 1.12、RMS −6.4 dBFS)
  - 加新的 808 音色要**一起補進 E808_TRIM 並重量響度**,漏掉的那顆會特別吵

VCSL 看起來像是鼓，其實是管弦打擊樂（定音鼓、大鑼、碰鈸），不要拿來當爵士鼓用。

每一份檔案都走 `fetchDecode()` 的多來源清單：jsDelivr →
raw.githubusercontent（鋼琴多一條 tonejs.github.io）。同一份檔案不同路由，
新增音源時照這個模式走，不要只寫一個網址。

## 一小節不一定只有一顆和弦
「一個和弦一小節」是最常見的情況,**不是規則**:`C Am | F G` 這種一小節兩顆的寫法
在流行歌裡到處都是。所以資料結構是**每一格帶一串和弦**。

- **文字語法:「|」是小節線**。有寫 `|` 的時候,一條線之間的和弦擠在同一小節裡;
  **沒寫 `|` 的時候照舊「一個和弦一小節」**。用「有沒有 |」切換而不是換一套語法,
  舊的進行、存檔、分享連結才全部還能用——`groupsToText()` 也是這個規則:
  全部一格一顆就寫回 `C - Am - F - G`,有任何一格兩顆才寫 `C Am | F G`
- 一小節最多幾顆 = **拍數**(4/4 四顆、3/4 三顆、6/8 兩顆)。再多就沒有一顆音的時間
- **拍數平分,分不完的前面拿多的**(`spansOf()`):4/4 兩顆 = 2+2 拍、三顆 = 2+1+1、
  3/4 兩顆 = 2+1。那是真的譜上的寫法,一小節兩顆時第一顆通常撐得比較長
- **分析一律看攤平的那一串**(`flatChords()`):判調、判曲風、移調拼字、代理和弦
  都跟小節線無關——`C Am | F` 的 Am 後面就是 F
- **排程看 span**(`spanAt(spans, slot)`):
  - 鋼琴左手在**每一顆和弦的開頭**落一次,撐到那顆結束;右手打到哪一格就用那一格的和弦
  - 貝斯把**每一顆和弦各自當一個小節看**(`scheduleBass` 的 `span` 參數):
    第二顆的第一拍就是它的「第一拍」,該彈根音,不是接著前一顆的五音。
    一格只有一顆時 `b`/`beats`/`mid` 全部跟以前一模一樣,**零行為差異**
  - **例外是放克與 city pop 的十六分切分**:那是寫給整小節的格子(`[0,3,10]`),
    要用**絕對**格號。跟著和弦重新從 0 數的話,第二顆會去對整小節的格子,
    大半個小節就沒聲音了
- **編輯的單位是一顆和弦,不是一小節**:選取、鍵盤、音名/性質都對到攤平的序號
  (`chordTokens()` / `posOf()` / `setChordAt()`)。小節格裡每一顆各自是一個按鈕,
  兩顆中間畫一條線
- 鍵盤:`←` `→` 一顆一顆走;**數字鍵跳的是「第幾格」**(那是使用者數得出來的東西),
  跳到那一格的第一顆
- 工具列因此變成**四顆**:插一格、刪一格、**切兩顆/合併**、復原。
  原本寫「只有三顆」是因為當時第四顆做不到別的事;「把一小節切成兩顆」
  除了自己在文字框打 `|` 之外沒有別的路,所以它通過了「沒有它就做不到嗎」那一關。
  加上去之後那一排會擠成兩排(整頁多 25px),提示字要跟著縮短

## 兩個軸:感覺先由和弦決定,疏密歸使用者
不要把「曲風」做成**必選**的選單。一開始讓使用者直接挑 Bossa/Pop/Rock/Smooth Jazz,
結果是 8 組進行 × 4 種風格裡一堆說不通的組合(Dm9-G13-Cmaj9 套 Rock、
C-F-G-F 套 Bossa)。「要用哪種感覺」本來就是和弦決定的,所以**預設是自動**;
選單只是覆寫,不是入口。

- **感覺**(`FEELS`,預設自動):`detectFeel()` 看和弦語彙——任一顆有九度以上延伸音 → swing、
  **一半以上(含一半)**的和弦有六度七度(屬七不算) → latin、其餘 → straight。
  決定 swing 比例、鼓打什麼、貝斯走法、comping 位置
- **曲風可以覆寫,但自動是預設**(`feelPick`:`auto` 或六種曲風的 key)。`feelPick==="auto"` 時 `feelNow` 跟著和弦跑,指定之後就鎖住,
  換和弦也不會被推翻——`C - Am - F - G` 想用搖擺彈(或反過來,拿 ii-V-I 練直的八分)
  是真的需求,自動判得再準也蓋不掉這件事
- **畫面上寫的是曲風名字,不是「直/拉丁/搖擺」**:`Pop` / `City Pop` / `K-Pop` /
  `Bossa` / `Blues` / `Swing` / `Funk`,
  一律不翻譯。原本寫中文的「直」根本看不出是什麼(八分的流行搖滾?),
  「拉丁」又比實作寬——裡面打的就是 bossa nova 的 clave、框邊小鼓、低音提琴,
  不是騷莎也不是曼波。Swing 與 Funk 在台灣的樂手之間本來就講英文,翻成中文反而要想一下。
  **程式裡的 key 不要跟著改**(`straight` / `latin` / `swing` / `funk` 照舊;
  City Pop 是 `citypop`、K-Pop 是 `kpop`):
  `GROOVES`、`COMP_CELLS`、`TEMPO_QUICK`、存檔、網址的 `f=` 全都用它,
  改 key 等於把舊的存檔與連結打掉
- 每顆都有 `title` 說清楚那是什麼(`feelStraightTip` 之類),因為一個英文字講不完
  「圓舞曲與 6/8 抒情也走 Pop 這一種」這件事
- **曲風那排在卡片最上面、全寬**,不在右欄。它是「這首要練什麼」的入口:
  選它會同時決定**伴奏怎麼打**與**段落庫列出哪些進行**。
  右邊那串狀態(小節數、形式、移調)併在同一行,省掉原本「和弦進行」那一整列——
  卡片最上面放一個標題寫「和弦進行」,下面就是和弦進行,那行字沒有在講事情
- **選單只有一個**。段落庫裡面**不要再放一次曲風分頁**:兩個一模一樣的選單會打架
  (一個換伴奏、一個換清單,使用者分不出差別)。清單跟著 `feelNow` 走就好
- **段落庫的數字要數「現在這個曲風有幾組」**,不是 `SECTIONS.length`。
  寫 54 會以為每個曲風都有 54 種進行(被問過:「不能全部曲風都是 54 種和弦進行」),
  實際上 Pop 23、City Pop 11、K-Pop 7、Bossa 8、Blues 6、Swing 14、Funk 5
- **曲風膠囊變暗時點下去要連拍號一起換**(在 3/4 點 Funk → 換成 4/4)。
  跟拍號那排同一條規則:變暗是提示不是鎖。擋住的話 Funk 那五組 4/4 的進行
  在 3/4 就永遠碰不到——清單跟著曲風走之後,擋住等於把整頁藏起來
- **「自動」不是一種曲風,所以不進那排膠囊**。前兩版都栽在這裡:
  先是把它當第五顆膠囊、寫成 `自動 · Pop`,被問「自動旁邊為什麼有 pop?」
  (旁邊就有一顆 `Pop`,同一個字出現兩次);改成「自動亮著、它判到的那顆加外框」
  之後,還是被問「自動我還是不知道是什麼風格」。
  現在的做法是七顆曲風膠囊加旁邊一顆獨立的 `.auto-chip`:
  - **實心金的那一顆永遠是現在在彈的**(`aria-pressed` 比的是 `feelNow`,不是 `feelPick`)。
    看曲風只要看哪一顆是實心的,不用先搞懂「自動」是什麼
  - 自動那顆只回答一件事:「這一顆是跟著和弦挑的嗎」。亮著用**空心金**,
    不跟實心的那顆搶
  - 加曲風的時候不要順手把它塞回那排膠囊裡
- 不要發明新顏色:整套工具的變數只有 gold / ivory / muted / line / danger,
  多開一個色階六個工具都要跟著改
- **那排不要標題**。標過「感覺」(被問「感覺是指什麼」)也標過「曲風」
  (被說「看右邊那幾個我知道是曲風」)——`Pop` / `City Pop` / `K-Pop` / `Bossa` /
  `Blues` / `Swing` / `Funk` 自己就說明了那是什麼,再加兩個字只是多佔一欄。
  拍號那排要留「拍號」是因為 `4/4` 單獨看不出是在講什麼
- 這幾條取代了原本「感覺畫面上不顯示,不要加標籤」那句:不顯示的前提是不能改,
  一旦能改就一定要看得到現在在彈哪一種
- 門檻是「一半」不是「超過一半」,那是刻意的:`Cmaj7 - A7 - Dm7 - G7`(bossa 的
  I-VI7-ii-V)與 `Fmaj7 - E7 - Am7 - C7`(city pop)都剛好是 2/4,兩個都該是拉丁。
  但「只有一顆」一定不算:`Em - D - C - B7`(安達魯西亞終止)、
  `C - Am - F - G7`(收在 V7 的流行進行)都不該變成 bossa。延伸音則是有一顆就算,
  九度以上很少不小心出現
- **伴奏樣式**(`PATTERNS`,使用者選):pad / comp / drive,只管疏密與力度倍率
- **疏密是使用者那一軸的,別人不准動**。段落庫的每一組進行曾經自己帶一個
  建議的疏密,點下去就 `setStyle(ex.style)`——結果是選了「混合」再挑一組進行,
  混合被換成「打點」,聽起來就像混合壞了(被回報「混合的伴奏模式失效了」)。
  已經把 `SECTIONS` 的 `style` 欄位整個拿掉:曲風與拍號是這首歌的事實,疏密不是
- **混合**(`mix`)**不是第四種疏密**,是「每繞一圈換一種」:`MIX_CYCLE` 走
  pad → comp → drive → comp,單位跟力度弧線 `ARC` 一樣是「一圈」(進行的長度),
  所以疏密跟力度是一起起伏的(0.9 / 1.0 / 1.1 / 1.0)。
  換的時機一定落在進行的開頭,不會在樂句中間忽然變密。
  它是由那三種組出來的,所以九種組合一個都沒少,只是會依序走過其中三種
- **排程裡一律問 `styleAt(absBar)`,不要再直接比 `styleOn`**。漏掉的地方混合就不會變,
  而且是「只有混合會怪、其他三種都正常」的那種漏——很難發現。
  `pat()`、`bassMode()`、`compPositions()` 都吃 `absBar` 就是為了這個
- 兩個在排程時交叉(`feel()` × `pat()`)。加新東西時想清楚它屬於哪一軸;
  九種組合每一種都要成立
- `drive` 會把貝斯往上推一級:straight → 八分推進、swing → 走動低音,
  latin 維持 bossa 的形狀(那個切分就是它的味道,改掉就不是 bossa)
- **七種曲風,但只有三種推得出來**。`detectFeel` 只回 straight / latin / swing;
  **City Pop、K-Pop、Blues、Funk 永遠不會被自動選到**,只能自己點或從段落庫點進行帶進來:
  - City Pop 的 maj7 / m7 / 二級屬跟 Bossa 是同一套語彙,差別在律動不在和弦
  - K-Pop 的 vi-IV-I-V、4536、卡農是全世界流行歌共用的,跟 Pop / City Pop 分不出來
  - Funk 的 9、13、7♯9 跟爵士同一套語彙,`Em9 - A13` 看和弦分不出是誰
  - Blues 的 `C7 - F7 - G7` 跟「直的 12 小節藍調」一模一樣,差別在八分是不是三連音
  這不是缺陷,是這四種本來就定義在律動上。**不要為了讓它們能被自動判到
  而去猜和弦**(例如「全是屬七就當 Blues」——那樣爵士藍調會被搶走)
- 細分走 `sub`:`FEELS.funk.sub` / `.citypop.sub` / `.kpop.sub` 都是 2,把一拍切成四格
  (4/4 一小節 16 格)。`subOf()` / `divNow()` 是唯一兩個看它的地方,
  `slotTime()`、`slotsInBar()` 之類的都吃 `divNow()` 所以自動跟著變。
  **加新曲風要細分也走 `sub`,不要另外寫一套格子**
- **City Pop 跟 Funk 差在 hi-hat 走幾分**:Funk 走滿十六分、City Pop 只走八分,
  把十六分留給貝斯與大鼓去咬。乾淨的 8 beat 骨架配十六分的低音律動就是 city pop;
  hi-hat 一走滿就變成放克了
- **Blues 是三連音不是十六分**:`swing: 0.66`(爵士是 0.58,藍調的搖擺更接近正三連音),
  格子跟 Pop 一樣八格,靠 `slotTime()` 把後半格推過去。6/8 就是 12/8 的慢藍調,
  那裡三連音是拍號自己就有的,不再另外搖
- **哪個曲風在哪些拍號成立寫在 `FEELS[x].meters`**,沒寫就是三個都可以
  (Funk、City Pop、K-Pop 只有 `4/4`,Blues 是 `4/4` 與 `6/8`)。`feelFitsMeter()` 是唯一的判斷:
  膠囊變暗、指定被擋、換拍號退回自動全都走它。**不要再針對某一種曲風寫 if**——
  第三個受限的曲風進來時一定會漏掉一邊,而漏掉的症狀是「格子數對不上、整小節沒聲音」
- **鋼琴左手在 boogie 底下要再讓一階**(`lhGain()` 回 0.38 不是 0.55):
  貝斯一小節走八顆(根-五-六-五),左手同時撐一顆長根音,兩層低頻疊在一起會糊。
  藍調鋼琴本來就是左手自己走 boogie 或不碰低音

## 繞第二圈就換和弦(代理和弦)
同一個四小節繞二十次,和弦一模一樣會很悶。`reharmOn`(開關「和弦變化」,預設開)
每繞一圈換一兩格。

- **換的是代理和弦,不是把 7 改成 9 再改成 13**。被退過一次:
  「替換和弦並不是光更改 7、9、11、13 的數字部分,我更希望的是替代和弦」。
  `SUB_RULES` 裡是真的會寫在譜上的手法:I 用 vi/iii 代、IV 用 ii 代、
  IV 借小調的 iv、要去 IV 之前先變 I7、下一格的副屬和弦、V7 的三全音代理、
  兩格同一顆就把前面那格變成它的 ii、經過減和弦、一轉與屬七的三轉。
  加色彩音只留**一條墊底**,上面都不成立時才用
- **代理是講級數的事,所以要先知道調**(`keyOf()`)。判調只看和弦的**骨架音**
  (根音、三度、五度、大七;`KEY_CORE`),**降七度與延伸音一律不算**——
  藍調的 C7 F7 G7 全帶降七度,把它算進去的話 F 大調會贏(C7 的 B♭ 在 F 大調裡),
  F7 就變成 I 級,整組規則跟著錯位。再加「主和弦出現幾次」當配重
  (小調算關係小調的 vi)。54 組全部判得回 C 大調 / A 小調
- **只有爵士系(Bossa / Swing)會整顆換掉別人**(`JAZZ_FEELS`)。
  副屬和弦、三全音代理、補 ii 這三條只給它們;
  Pop / City Pop / Blues / Funk 只做「加色彩音、轉位、三度代理、借用 iv、I7 接 IV」
  這種不動骨架的事。爵士本來就是邊彈邊重配,流行不是。
  被回報過:City Pop 的 `Fmaj7 - E7 - Am7 - C7` 被換成 `Fmaj7 - B♭7 - Am7 - C7`——
  那是 E7 的三全音代理,理論上完全正確,但 E7 正是丸サ進行的招牌,換掉就不是那首了
- **兩種「這一顆不要動它的根音」**(非爵士系才套,`keepRootOnly`):
  1. **調外的和弦是作者故意寫的**:丸サ的 E7、藍調的 C7 都帶調外音,那是招牌
  2. **上一格的屬七要解決到這裡**:`E7 - Am7` 的 Am7 換成 Cmaj7 的話,
     前面那顆 E7 就解決不到任何地方,聽起來會懸在半空
  兩種都只擋「換根音」的規則(`keepsRoot`),加色彩音與轉位照舊。
  經過和弦(`passing`:pass° / ♯IV°)也不受限——它們要求前一格是同一顆,
  換的是重複的那一格,沒有拆掉任何東西
- **不要用「理論上對」當標準,要用「這首歌還是不是這首歌」**。
  代理和弦的規則書上每一條都是對的,但套在別人的招牌上就是錯的
- **最有用的一條是「副屬化」:根音不動,只把性質改成屬七**。
  下一格的根音在自己的完全四度上方時,這一格就可以當它的 V7——
  `F - G - Em - Am` 的 **Em 隨時可以彈成 E7**、`… Am - Dm …` 的 **Am 可以彈成 A7**、
  要去 F 之前的 C 可以彈成 C7。使用者自己指出來的,而且說「隨時可以換」。
  它**低音一個音都沒動**,所以永遠安全:招牌和弦、屬七要解決過來的那一格都照給。
  小三換成大三那一下就是它的味道,所以三和弦的進行也給一個純大三的選項(Em → E)
- **換根音的規則要少、不換根音的規則要多**。使用者要的是「聽起來還是同一首,
  但是有變化」,不是每一圈都在重配和聲
- **♯IV 減七要比級數不是比音程**:條件是「IV 走回 I」(藍調第六小節),
  只比「下一格在五度上」的話 `C7 C7 G7` 也會被塞一顆 C♯dim7,那不是經過和弦
- 「加色彩音」那條**要用音級集合守門**(`progPcs` / `extFits`):
  `C - G - Am - F` 的 G 套「大三 → 大七」會變成 Gmaj7(帶 F♯)。
  真正的代理和弦相反,副屬與三全音代理本來就是要帶進調外音,不能拿同一把尺量
- **轉位要保留原本的性質**:`x.full` 不是 `x.text`。用根音串接的話
  `Dm7` 會變成 `D/F`(大三和弦!),整個和弦的顏色就換掉了
- **轉位只在低音因此級進時才給**(`stepToNext()`)。被回報過「很多 slash chord 很難聽」:
  轉位存在的理由就是把低音線接順(`C G/B Am` 的 B→A),
  隨便把三音丟到低音只會得到一條亂跳的低音線。屬七的三轉(G7/F)同一條規則——
  它在王道的 `Fmaj7 - G/F - Em7`(F→E)成立,在別的地方就不成立
- **vi → I 要這首真的出現過 I 才給**:`Am - F - G - Am` 的 Am 是主和弦,
  換成 Cmaj7 等於把主音抽掉
- **換完之後不可以跟左右鄰居變成同一顆**(`clashesNeighbour`)。
  被回報過「有些替代和弦聽起來很怪」,追下去大半是這個:
  `Fmaj7 - Fm7 - Em7 - A7` 的 Fmaj7 套「借用 iv」變成 `Fm - Fm7`、
  `… Fmaj7 - Dm7 - G7` 的 Dm7 套「ii→IV」變成 `Fmaj7 - Fmaj7 - G7`——
  兩個都是**把一次和弦變化抹掉**,聽起來就是少了一格。
  比的是「根音 + 性質」不是字面(`Fm` 跟 `Fm7` 是同一顆)。
  **只擋換根音的規則**:加色彩音、轉位、副屬化(`keepsRoot`)本來就不動根音,
  `C - C - C7` 換成 `C - Cmaj7 - C7` 是很好聽的線性進行,不該被擋掉
- **加色彩音只能往上加,不能往回拆**。原本 `EXT_TABLE` 裡有 `"m9": ["m7"]`,
  結果 `Dm9 - Dm9` 會被換成 `Dm7 - Dm9`——那不是變化,是把色彩音拿掉再加回來,
  聽起來像彈錯。要動就往 `m11` 走
- **「借用 iv」要接得到 I 或 V 才給**:`IV - iv - I` 是它的本家寫法(F - Fm - C),
  接 V 也成立。接到別的地方只是把大三換成小三——
  `Am7 - G - Fmaj7 - E7`(安達魯西亞)的 F 換成 Fm,那條下行線就沒了
- **「補 ii」要那顆屬七真的往下五度解決**(看**這一對後面那一格**的根音)。
  `C7 C7 F7` → `Gm7 C7 F7` 成立(爵士藍調第四小節的本家寫法);
  但藍調第五、六小節的 `F7 F7 C7` 裡 F7 是 IV 不是 V——
  塞 Cm7 進去會變成 B♭ 的 ii-V 卻走到 C7,整組懸在半空(踩過)
- **ii-V 的 ii 絕對不可以變成屬七**。被回報過:王道進行
  `Fmaj7 - G7 - Em7 - Am7 - Dm7 - G7 - Cmaj7 - C7` 的 Dm7 被「副屬化」換成 D7
  (「後半段 2-5-1 這麼標準的和弦進行都可以搞錯」)。
  副屬化的條件是「下一格的根音在自己的完全四度上方」,而 ii-V 剛好長這樣
  (D → G),所以它會一路踩中每一個 ii-V。
  規則加一條 `x.nq !== "dom"`:**下一格已經是屬七的時候,這一格就是它的 ii**,
  ii 是小三和弦才叫 ii-V,改成屬七就變成 V/V-V——那是另一組進行,不是同一組的代理。
  74 組裡「下一格是屬七」的案例全部是這件事:`Dm7 - G7`、`Am7 - D7`、`Em7 - A7`、
  `A♭m7 - D♭7`,一顆都不該動。爵士的連續屬七(E7-A7-D7-G7)理論上成立,
  但那是**重配**不是代理,要的話走 `V7/next`
- **副屬化不可以把色彩音拆掉**(`domSuffix()`):`Am9 → A7` 少了九度,
  跟 `EXT_TABLE` 那條「只能往上加」是同一件事。原本有九度以上的就保留一階——
  **往下走去小和弦給 `7♭9`**(小調的 V7 本來就帶 ♭9,和聲小音階自己就有),
  去大和弦給 `13`
- **`EXT_TABLE["13"]` 是空的**。原本寫 `["9"]`,那是把十三度拆掉,
  跟被拿掉的 `"m9": ["m7"]` 一模一樣。屬十三再往上只剩 ♯11 / ♭9 那種改變功能的音,
  那不是換聲位而是換一顆和弦
- **三全音代理的根音一定拼降記號**(`FLAT_ROOTS` / `flatNameOf()`)。
  它存在的理由是低音**往下半音**接到目標(G7 → D♭7 → C、C7 → G♭7 → F),
  `nameOf` 走的 `PAL_ROOTS` 在 pc 1 與 6 是 `C♯` / `F♯`,寫出來那條半音線
  在譜上變成往上的,看起來是另一回事。升記號留給真的往上走的經過減七
  (`C♯dim7 → Dm7`、`F♯dim7`)。**其餘十個音級本位名就是對的**——
  pc 11 的 `B7`(接 B♭maj7)、pc 2 的 `D7`(接 C♯dim7)都不該硬加降記號
- **低音正在走一條級進的線時不要動根音**(`bassLine`)。
  卡農 `C - G/B - Am - Am/G - F - C/E - Dm7` 的 F 換成 Dm7,低音 G→F→E
  就斷成 G→D→E;安達魯西亞 `Am - G - F - E7` 同理。
  這是原本那句「F 換成 Fm 下行線就沒了」的一般化,也是 `stepToNext` 的反面:
  轉位存在的理由就是把低音線接順,**那條線比「換一顆和弦」重要**。
  前後都級進、而且同一個方向才算;不動根音的規則(借用 iv、加色彩音、轉位、
  副屬化)不受限,那條線原封不動
- **旁邊那格「根音一樣但和弦不一樣」時不要動根音**(`pairedRoot`)。
  `Am7 - D7 - Dm7 - G7`(City Pop)的 `D7 → Dm7` 是刻意在同一個根音上換顏色,
  把 Dm7 換成 Fmaj7 等於把那一下抹掉;`F - Fm - C` 的借用 iv、
  `Am7 - A7 - Dm7` 的副屬化都一樣,那兩格合起來才是一件事。
  **一模一樣的重複不算**:`C - C - F - G` 的第二格換成 Am7、
  Blue Bossa 撐兩小節的 `Dm7 - Dm7` 換掉前一格,那正是代理和弦該做的事。
  踩過:第一版寫成「根音一樣就擋」,連重複的主和弦都不能換了
- **「上一格要解決到這裡」有三種,不是一種**(`resolvesHere`):
  屬七往下五度(`G7 → Cmaj7`)、屬七往下半音(三全音代理 `D♭7 → Cmaj7`)、
  **減七往上半音(導音和弦 `C♯dim7 → Dm7`)**。原本只認第一種,
  結果 City Pop 的 `C♯dim7 - Dm7 - G7` 裡 Dm7 套「ii→IV」變成 Fmaj7,
  前面那顆經過減七就解決不到任何地方——它存在的唯一理由就是半音走上去接到 D
- **這一整組規則要用機器去驗,不要靠讀**(scratchpad 的 `theory.mjs` / `fuzz.mjs`)。
  八條斷言:不可以跟原本一樣、副屬化的下一格一定在四度上且不是屬七、
  三全音代理一定在目標上方半音且不拼升記號、轉位的低音一定級進且不換性質、
  減七一定是經過和弦(導音往上半音**或**共同音減七從下面半音來,
  藍調第六小節的 `F7 - F♯dim7 - C7` 是後者)、不可以拆掉色彩音、
  換根音的不可以跟鄰居同一顆、前一格要解決過來時不可以換根音。
  74 組 402 格加上 1200 組亂數進行、12078 個候選,現在全部通過
- **第一圈(lap 0)一定照原譜**,之後一圈換一到兩格,第 1 格盡量不動
- 換的時機在**圈的第一小節第一拍**(`buildLap()`),排程提前 0.2 秒
- **畫面要顯示真的在響的和弦**:`barsNow()` 是小節格、走帶列、排程共用的那一份
- 關掉開關、改和弦、按播放都要 `resetLap()`。**`resetLap()` 一定要排在
  `renderBars()` 前面**:漏掉的話會用「上一首的格數」畫格子、欄數卻照新的算,
  症狀是「8 格擠在同一橫排」(回報過)

## 聲位:色彩音不要擺在最上面
- `buildVoicing` 的成本函式對「頂音是九度以上」罰 7 分(約等於三個半音的聲部移動)。
  聽的人會把最高音當旋律,旋律停在 9 或 11 上就像唱到一半斷掉——
  被回報過「9、11、13 音放在最上方,聽起來反而不好聽」。
  罰分不是硬禁止:真的只剩這個轉位時還是彈得出來。54 組 328 格實測 0 格頂音是色彩音
- 六度(9)不算色彩音,那本來就是很好聽的頂音

## 伴奏密度與「像機器人」
- **推進(drive)是「八分音符不停」,不是「每一格都打」**。City Pop 與 Funk 是
  一拍四格(`sub:2`),照格子數走就變成一小節砸十六下(回報過「瘋狂砸和弦」)。
  步進一定要走 `subOf()`,十六分的曲風再把本家的切分疊上去
- **不是每一下都把整組音一起砸下去**:重拍落滿,中間的反拍多半只帶上面兩顆、
  偶爾只有最上面一顆(`schedulePiano` 裡依 `hash01` 決定)。
  每一下都一樣厚就是「機器人」的主要來源,比節奏型固定還明顯
- 加曲風或改密度之後要重量一次每小節的「敲幾下」:pad 1–2、comp 2.5–5、
  drive 5.5–8.5,七種曲風都要落在這個階梯上,差太多就是某一種特別吵
- **加曲風也要重量一次響度**:Rhodes 與 808 都會動到 `masterGain → 限幅器 → outGain`
  那條線。目標照舊(peak ≤0.85、RMS −12～−14 dBFS、零削波),
  現在量到的是 K-Pop 0.85–0.88 / −12.2～−12.4、City Pop(Rhodes)0.85–0.87 / −13、
  Funk(Rhodes)0.85 / −13.1

## 拍號是第三個軸,但它不是使用者要煩惱的東西
和弦決定感覺、使用者決定疏密,**拍號決定骨架**:一小節幾拍、鼓怎麼打、
貝斯的位置、過門在哪一拍。它推不出來(同一組和弦可以是 4/4 也可以是 3/4),
所以一定是使用者選的,但選完就不用再管——三顆膠囊,設定一次。

- `METERS` 只有三個:`4/4`(四拍,一拍兩格)、`3/4`(三拍,一拍兩格)、
  `6/8`(兩大拍,一拍**三格**)。
  **為什麼只有三個**:12/8 的慢藍調跟「4/4 + 搖擺」聽起來是同一件事(差在記譜),
  已經被搖擺那個感覺蓋掉了;2/4 的進行曲寫成 4/4 也一樣。
  5/4、7/8 要自己一整組節奏型才不會是湊數的,先不做
- **所有節奏型一律用「第幾格」寫**,不寫秒數也不寫拍數。一小節幾格 =
  `beats × div`:4/4 八格、3/4 六格、6/8 六格。
  **程式裡不可以再出現寫死的 4**(一小節四拍、count-in 八拍、`musicBeat / 4`),
  一律走 `beatsInBar()` / `slotsInBar()` / `beatSlots()` / `midBeat()`
- `slotTime()` 是唯一把「第幾格」換成時間的地方:單拍子的後半格要搖擺,
  **複拍子不搖**——6/8 的三連音是拍號本來就有的,再搖就變成搖擺的搖擺
- 感覺被拆成兩半:`FEELS` 是跟拍號無關的(搖擺比例、音量、用哪顆鼓、貝斯走法),
  `GROOVES[感覺][拍號]` 才是真的打在哪幾格。**換拍號不是把 4/4 的型剪掉兩格**,
  是換一種打法:圓舞曲的小鼓在二、三,6/8 的小鼓在第二大拍,
  爵士圓舞曲的 ride 是「1 2-da 3-da」,慢藍調的 ride 是 12/8 的 shuffle,
  拉丁的 3/4 與 6/8 共用同一組格子(`[0,2,3,5]`,非洲 6/8 的短鈴型)
- `COMP_CELLS` / `KICK_CELLS` / `SNARE_CELLS` 也都是「感覺 → 拍號 → 一堆型」。
  格子數對不上就整組打不進小節裡,加拍號時三張表都要補
- **每一組現成的進行都帶自己的拍號**(`SECTIONS` 的 `meter`),點下去連拍號一起換。
  同一串和弦在別的拍號當然彈得出來,但「這句歌本來是幾拍子的」是有答案的——
  丸サ進行沒有人用圓舞曲彈、Amazing Grace 不是 4/4。不要讓使用者自己猜
- 點完之後另外兩個拍號**變暗**(`formMeter` + `.dim`)。變暗是提示不是鎖:
  還是點得下去,點下去就當使用者自己要換,提示收起來(`clearFormMeter()`)。
  自己打字、清空、改長度、復原也一律收起來——那時候沒有「本來的拍號」可言
- 接段落時如果拍號跟現在這串不一樣,`formParts` 先清掉當新的一首。
  A 段三拍、B 段六八拍的曲子基本上不存在,硬接只會得到一串要自己清掉的東西
- 播放中換拍號會有一小節接不齊(排程只提前 0.2 秒),下一小節就回到正軌。
  這是刻意不處理的:多數人是按播放前就選好

## 速度跟感覺與拍號綁在一起
- 三顆快捷速度不是固定的 70 / 100 / 120,是 `TEMPO_QUICK[感覺][拍號]` 的慢/中/快。
  爵士圓舞曲本來就比中速搖擺快(♩=130–210)、bossa 在 110–150、
  6/8 的 50 就已經是抒情歌了。和弦一改、拍號一換,這三顆就跟著換
- **點曲風膠囊會把速度帶到那個曲風的中速**(見「每個曲風要有一個一聽就知道的聲音」
  那節)。這條是後來改的:七種曲風橫跨 55–180,速度不跟著走的話根本分不出來
- 除此之外**不會自己去動使用者打的數字**,只有「單拍子↔複拍子」那一下例外:
  那顆音符從四分變成附點四分,同一個數字不再是同一個速度,
  超出新拍號的建議範圍就換成中間那個值
- 速度前面那顆音符(`bpmNote`)要一起換:6/8 顯示 ♩. ,不然會以為 55 是四分音符

## 段落與形式
- `SECTIONS` 分 4 小節與 8 小節兩組,點一下把段落接到 `formParts` 後面,
  文字框永遠從 `formParts` 重組。使用者自己打字就清空 `formParts` 回自由模式,
  兩邊不能各自為政
- 12 小節藍調自己一區。它全是屬七和弦,`detectFeel` 的 `isDominant()`
  特別把屬七排除在拉丁訊號之外,不然整組會被判成 bossa
- **接段落時曲風不一樣也要當新的一首**(跟拍號同一條規則):A 段 City Pop、
  B 段 Blues 的曲子不存在,硬接的話曲風只會留下後面那一段的,前面那半聽起來就不對。
  `formFeel` 記著「本來是什麼曲風」,跟 `formMeter` 一起在自己打字/清空時歸零

## 節奏要換骨架不是加裝飾
- `COMP_CELLS` / `KICK_CELLS` 每種感覺一組,每小節用 `pickCell()` 抽一個。
  第一個是本家型 45%,其餘平分。不要退回「固定骨架 + 機率裝飾」,
  那樣聽幾小節就鎖定骨架了
- **拍子感的來源不能拿掉,但每種感覺的來源不一樣**:
  流行與放克是小鼓的 2、4 backbeat;bossa 是 clave;爵士是 **hi-hat 踏鈸**踩 2、4。
  即興的人靠那個抓拍子,不管換成哪一種都要一直在
- `arcGain()` 讓力度隨圈數起伏(0.9→1.0→1.1→1.0),一直同一個力度會很悶

## 鼓要照那個曲風真的打法,不是換速度而已
七種曲風的鼓是七種不同的打法,不是同一組鼓打不同的位置:

| | 鈸 | 2、4 | 小鼓 | 大鼓 |
| --- | --- | --- | --- | --- |
| Pop | hi-hat 走八分 | 小鼓 backbeat | backbeat + 幽靈音 | 一、三 + 切分 |
| City Pop | hi-hat 走**八分** | 小鼓 backbeat | backbeat + 十六分幽靈音 | 十六分切分,比放克少 |
| Bossa | hi-hat 走八分 | clave | cross stick 打 clave | surdo 一、三,很輕 |
| Blues | hi-hat 走八分,**是三連音的** | 小鼓 backbeat | backbeat + 幽靈音 | 一、三 |
| Swing | **ride** 打 1・2・2的後半・3・4・4的後半 | **hi-hat 踏鈸** | 稀疏 comping | feathering 四分,很輕 |
| Funk | hi-hat 走**十六分** | 小鼓 backbeat | backbeat + 十六分幽靈音 | 十六分切分,不落在正拍 |
| K-Pop | hi-hat 走**十六分** | **沒有人**(half-time) | **拍手只打第 3 拍** | 808,一小節兩三顆 |

- **K-Pop 是唯一沒有 2、4 backbeat 的曲風**,那是 half-time 的定義:整小節只有
  第 3 拍那一下。跟 Funk 的差別不在 hi-hat(兩個都走滿十六分),在「誰在數二四」——
  少了二四,同樣的速度聽起來慢一半。**不要為了「拍子感的來源不能拿掉」
  而幫它補一顆二四的小鼓**,補了就變成放克
- **backbeat 打的是 `f.snarePiece` 不是寫死的 `"snare"`**。Pop / Blues / Funk /
  City Pop 的 `snarePiece` 本來就是 `"snare"`(零差異),K-Pop 的是 `"clap"`。
  幽靈音那一條保持 `"snare"`:拍手沒有「很輕地補一下」這種打法
- **808 大鼓的尾巴 0.52 秒,一小節只打兩三顆**。照實錄鼓的密度打(一小節四五顆、
  尾巴 0.9 秒)會疊成一條不會停的低頻,整首 RMS 被它一個人拉高 5dB(量過)

- **會打很多下的鈸,強弱差距要拉大,不然就是吵**(被回報「Swing 跟 Blues 的打擊樂很吵」):
  ride 一小節六下、shuffle 的 hi-hat 一小節八下,取樣又是長的,
  一樣大聲會整片糊在一起。ride 的 skip note 收到 0.46(不是 0.6)、
  shuffle 的後半拍收到 0.45(不是 0.68),整體音量也各收一階
  (ride 0.38 → 0.30、Blues 的 hi-hat 0.3 → 0.24)
- **爵士的 ride 不是走滿八分**,是那個「ding ding-da ding」的型(`cymPattern:[0,2,3,4,6,7]`),
  後半拍那兩顆(skip note)要輕。走滿八分就變成搖滾的 hi-hat 加了搖擺,聽起來很怪
- **爵士的小鼓不是 backbeat**。打 2、4 是搖滾/shuffle 的打法;爵士的小鼓在做 comping,
  用 `SNARE_CELLS.swing` **平均抽**(`pickCellEven`,沒有本家型)——
  連三小節打同一個位置就變成節奏型,那就不是 comping 了
- **爵士的大鼓是 feathering**:四分音符、幾乎聽不見(0.16),`KICK_CELLS.swing` 抽到的
  那顆才是重音(丟炸彈)。推進不加「二的後半」那顆推力,那是搖滾的推法
- **bossa 的 clave 是兩小節一循環**:3 邊 `[0,3,6]` 與 2 邊 `[2,5]` 輪流。
  每小節都打同一邊就只是一個節奏型,不是 clave。而且要用 cross stick(框邊敲擊),
  不是一般的小鼓中心擊——bossa 從頭到尾沒有人在打小鼓中心
- **推進的推力也要照感覺換位置**:流行搖滾推「二的後半」、bossa 推「四的後半」
  (進下一小節的預備),爵士不推
- **搖擺的過門走三連音**(`spb/3`)不是十六分。八分是搖擺的、過門突然變成整齊的
  十六分,那一拍會整個掉出來
- **放克的 hi-hat 要有強弱**,不然十六分會變成打字機:正拍 1、後半拍 0.78、
  十六分那兩格 0.5(`div === 4` 時照 `k` 給),那個強弱差就是 funk 的律動來源。
  大鼓與貝斯的格子刻意都不落在第二、第四拍的正拍上(`[0,3,10]`、`[0,6,10,14]`),
  空出來的地方才是 backbeat 跟切分聽得出來的原因
- 鼓件依感覺載:`FEEL_PIECES` 說這個感覺要哪幾顆,`ensureDrums()` 會等它們
  (ride 換成合成音就不是爵士了)。ride 兩顆 round-robin 共 2MB,
  只有搖擺的進行才會下載。放克用的是基本三件(`FEEL_PIECES.funk` 是空的),
  不會多載任何東西

## 力度要改的是音色,不是音量
「聽起來像 MIDI」有一半來自這件事:每一件樂器都只有一層取樣,強弱全靠 gain。
真的樂器打輕不是「同一個聲音小聲一點」,是**整個音色變暗**。

- **鋼琴**(`playNote` 的 `velRef = 0.42`):力度換算成一顆低通的截止頻率
  (`700 + 16000 × vel^1.45`)。**0.88 以上整顆濾波器跳過不建**——
  那時截止已經在 14kHz 以上,取樣本身就沒有那麼多高頻,而最大聲的那幾下
  正是同時發聲最多的時候,省下的就是那時候的節點
- **貝斯**(`playBassNote` 的 `bVelRef = 0.56`,`500 + 6000 × vel^1.8`):
  走動低音的經過音、放克的幽靈音都是照 gain 調小的,撥弦聲(2–4kHz)不跟著縮
  就是一台 MIDI 貝斯。0.56 是量出來的「用力撥」的 peak
- **真的多力度層只有鼓做得起**。Salamander 一顆音一層 1.5–2.5MB,
  四層 × 七顆音是 50MB 下載、解碼後兩百多 MB 記憶體,手機會直接掛;
  鼓一顆幾十 KB 才付得起(見上面 `DRUM_LAYERS`)。
  **不要因為鼓做了就順手把鋼琴也改成真力度層**
- **強弱的範圍也要拉開**(`schedulePiano` 的 `accent`):
  小節第一下 1.35、每一拍的拍頭 1.0、反拍 0.62、十六分那兩格 0.45。
  全部打同一個力度的話,前面那兩顆濾波器等於沒有——先有動態才有音色變化

### 貝斯也要 round robin
走動低音一小節四顆、boogie 一小節八顆,同一個音高一直播**同一份**取樣,
是「機器在按」最聽得出來的地方——真的貝斯手不可能兩次撥得一模一樣。
兩把琴都錄了好幾份(電貝斯 `_rr1..4`、低音提琴 `_ma..md`),各取三份。
**第一份擋住播放、其餘跟鼓一樣背景補**(`drumFillGate`):
擋住的部分量到 650–670ms,背景補完之後每個音有三份。

## 殘響的雜訊也要固定種子,而且左右要不一樣
`buildImpulseResponse` 原本用 `Math.random()`,所以**每次重新整理殘響就是另一條尾巴**,
量到的尖峰跟著飄——那正是「同一段量兩次差 0.14」那條規則要擋的事,只是漏了這一處。
改成 `mulberry32`(跟 `noiseBuf` / `hash01` 同一套),現在同一段量兩次差 ≤0.005。

- **左右兩聲道要餵不同的種子**。同一份雜訊給兩邊的話整片殘響會塌回正中間,
  聽起來是「加了一個效果」不是「在一個空間裡」。早期反射也左右錯開 3.1ms、極性相反
- 整份檔案現在**一個 `Math.random()` 都沒有**,加東西進來也不要再引進

## 伴奏團不是只有鋼琴鼓貝斯
「多一點樂器」不是多開幾顆開關,是**每個曲風本來就有它的編制**。
第四層寫在 `FEELS[x].parts` 裡,跟 `piano` / `drums` / `kit` 同一個做法:

| 曲風 | 吉他 | 鍵盤鋪底 | 打擊小物 |
| --- | --- | --- | --- |
| Pop | 鋼弦刷弦(下刷正拍、上刷反拍) | — | 鈴鼓疊在 backbeat |
| City Pop | 乾淨電吉他十六分切音 | 弦樂 | 沙鈴走八分 |
| K-Pop | **不給** | **supersaw + 低音鋪底** | 沙鈴 + 指響(疊在拍手上) |
| Bossa | **尼龍弦**打 clave | — | cabasa 走八分 |
| Blues | **不給** | Hammond 風琴 | — |
| Swing | archtop 四分音符(Freddie Green) | — | — |
| Funk | 悶音切音 | Hammond(很輕) | 鈴鼓走八分 |

- **和聲完全沿用鋼琴算好的那一組**(`guitarNotes(sp.v)` 只是把同一組音搬進吉他音域)。
  兩件和聲樂器各配各的一定會打架,而且跟著彈的人會聽到兩個版本的和弦
- **疏密照樣走 `styleAt(absBar)`**:墊底時吉他一小節刷一下、推進才走滿。
  九種組合每一種都要成立
- **bossa 的吉他跟 cross stick 共用 `g.clave`**。兩層打不同的格子會互相抵消,
  clave 就消失了——那是 bossa 唯一的拍子來源
- **K-Pop 不給吉他、Blues 不給吉他**:前者的骨架是 808 加合成器、
  後者的鋼琴已經在走 boogie,再塞一把切音會把中頻堵死。
  「每個曲風都加滿」不是配器,是把東西堆上去
- **音量比鋼琴低一階**,而且新加的三條 bus(`combBus` / `padBus` / `percBus`)
  殘響量各自不同:吉他一點空間感、鋪底要濕、打擊幾乎全乾
  (沙鈴一有殘響就變成噴氣聲)
- **鋪底不要跟著節奏打**:一顆和弦落一次、撐到那顆結束。它一有節奏就變成
  第二台鋼琴,整個伴奏會擠

### 撥弦最容易錯的是**音準**,不是音色
`ksBuffer` 的延遲線長度一定是整數,所以要用 `playbackRate` 修音準。
**但迴圈裡那顆低通自己也有群延遲**:`y[n] = damp·x[n] + (1−damp)·y[n−1]`
在直流的群延遲是 `(1−damp)/damp` 個取樣,所以真正的迴圈長度是
`L + (1−damp)/damp`,不是 L。

漏掉它的症狀是**整把吉他偏低,而且越高音偏得越多**:實測 steel 在 E2 只低 2.4 音分、
E5 就低 20.5 音分,archtop 更誇張到 40 音分。一個刷弦和弦裡低音幾乎準、
高音低二十音分,聽起來就是走音——被回報過「Pop 的吉他聲聽起來很怪」。
**`damp` 一改就要重量一次音準**(scratchpad 的 `pitch.mjs`,自相關量實際基頻)。

### 吉他的聲位要**照六條弦排**,不是把鋼琴那一組搬過來
一開始是拿 `v.upper`(鋼琴的密集四音)直接彈,結果像曼陀林不像吉他。
鋼琴的聲位是四個音擠在一個八度內,吉他的和弦是**攤開在兩個八度上**的。
`guitarVoice()` 從低音往上、每一條弦挑下一個和弦音,低音區至少隔四個半音、
高音區才可以靠近——`Am7` 會排成 `A2 E3 A3 C4 E4`,那就是開放把位的 Am。

- **音級還是同一組**(都來自同一顆和弦),所以跟鋼琴不會打架。
  要避免的是「兩個樂器各配各的和弦」,不是「兩個樂器用同一個聲位」
- 每個曲風的形狀不一樣(`guitarShape`):刷弦五條、bossa 四條、
  Freddie Green 三條、**切音三條而且不彈根音**(貝斯已經在彈根音了,
  吉他再彈一次只是把低頻塞滿——Nile Rodgers 的和弦幾乎都沒有根音)
- **每條弦要差個兩三音分**(`pluck` 的 `off` 表)。真的吉他不可能六條弦都完全準,
  全部彈得一模一樣準是合成器才有的事
- **撥片碰到弦那一聲不能省**(`T.pick`)。KS 本身只有「弦在振動」,
  沒有「東西碰到弦」——少了它聽起來是撥弦合成器,不是有人拿撥片在刷
- **刷弦的型要是真的刷法**:打點走 `D · D U · U D U`(八格的 0 2 3 5 6 7),
  不是「每一拍下刷」。每一拍都下刷聽起來像節拍器

### 鼓要像「一組鼓」不是「一堆取樣」
- **力度層是真的錄音,不是把音量調小**(`DRUM_LAYERS` / `pickDrumLayer`)。
  Virtuosity Drums 每顆鼓錄了好幾層(小鼓 32 層、大鼓與 hi-hat 4 層、
  框邊與 tom 16 層、ride 與 crash 3 層),每層還有 4 個 round robin。
  原本只取一層靠 gain 調大小聲,那是**最像 MIDI 的地方**——
  真的鼓打輕不只是小聲,泛音與殼的共振整個不一樣
- **力度層自己帶著音量差,所以 gain 要往回補一點**(`layerNorm`):
  輕的那層本來就錄得小聲,再照 gain 打一次會輕到聽不見
- **空間麥克風(`room/`)不是殘響**。人造殘響給的是「一個空間」,
  房間麥給的是「這一組鼓真的在那個房間裡」——鼓皮的餘韻、其他鼓件的共鳴、
  地板的反射都在裡面,合成不出來。它走自己的 `roomBus` 再進黏著壓縮
  (真的錄音就是這樣混的:房間麥被壓得越多,鼓聽起來越大越有空氣)
- **空間麥克風只抓中間那一層**,力度的差別在近麥已經有了;
  再乘一輪等於把下載量翻倍(ride 一個檔案就 1MB)。
  `playDrum` 會從整顆鼓裡找一個有 room 的層來借
- **`SRC_DRUMS` 停在 `Samples/`**,麥克風位置由呼叫端接。
  寫到 `Samples/mid/` 的話加 room 之後路徑會變成 `Samples/mid/mid/…`,
  整組鼓靜悄悄一個都載不到——而且 catch 是空的,只會默默退到合成鼓。踩過
- **載入要分三段,不然第一次按播放要等十幾秒**:
  1. 擋住播放的只有「中間那層的一兩個 round robin」(ride 與 crash 標 `heavy`,
     只抓一個——它們一個檔案 1MB,全抓是 26MB)
  2. 其餘力度層與 round robin 背景補
  3. **背景那一大包要等「真的開始播了」才放行**(`drumFillGate`):
     瀏覽器的連線是共用的,一按播放就灌十幾 MB 會把同時在抓的鋼琴與貝斯擠掉,
     有些曲風七秒還沒開始播(踩過)
- **鼓件要擺開**(`DRUM_PAN`):hi-hat 偏右、ride 偏左、tom 一高一低分開,
  打擊小物擺在 hi-hat 的**另一邊**(兩個都在右邊高頻會擠成一坨)。
  幅度都在 ±0.3 以內——這是把鼓組擺開,不是做效果
- **鼓的總線要掛一顆黏著壓縮**(`drumGlue`,3:1、起音 10ms、放開 120ms)。
  它不是拿來壓音量的,是讓每一顆聽起來像同一個房間裡的一組鼓

### 吉他改成真的取樣,KS 退成備援
被回報「Pop 的吉他聲很不真實,我要真實的吉他音色」。KS 那三件事
(撥弦位置的梳狀濾波、激發低通、迴圈低通)全部做對、音準也修到 1.7 音分之後,
還是少了**木頭箱體的共鳴**與**手指摩擦弦的聲音**——那兩件合成不出來,只能錄。

- 三把琴走 Tone.js instruments 的 mp3(`GTR_SRC` / `GTR_TABLE`):
  `acoustic` 鋼弦 D2–D5 **每個半音都有**(Pop 刷弦)、`nylon` B1–A♯5(Bossa)、
  `electric` C♯2–C6 每三個半音一顆(City Pop 切音、Swing 的 Freddie Green、Funk 悶音)。
  **mp3 不是 FLAC**:舊 iOS Safari 對 FLAC 的支援本來就差(鼓那一組踩過)
- **`GTR_NOTES` 要照真的檔案列,不要照公式推**:三把琴的取樣點完全不一樣,
  尼龍弦還有洞(C2、C♯2、G4 都沒有)。而且原始檔案表裡 `G5` 指到 `G3.mp3`
  (他們自己寫錯),收進來 G5 會低兩個八度——刷弦會冒出一顆莫名的低音,所以不收
- **KS 不刪,它退成備援**(位置跟 `synthDrum` 一模一樣):取樣載不到、
  超出錄音音域、或差超過三個半音時 `pluck` 自己掉回 KS。
  音色不對但不會沒聲音——「取樣一定載得到」這個假設從來不能做
- **差超過三個半音就不要硬拉**(`gtrSample`):`playbackRate` 拉太多會把
  木頭箱體的共振一起搬走,聽起來像玩具琴
- **`bright` 那顆低通是為 KS 訂的,取樣要另一個值**(`sbright`,預設 `bright × 1.9`)。
  合成的弦高頻太多太齊所以要收,真的錄音本來就是對的,同一顆低通會把空氣感切掉
- **撥片那一聲(`T.pick`)在取樣這一路要關掉**:真的錄音自己就有,再加一層
  就變成兩次觸弦
- **取樣要自己收尾**:KS 的 buffer 本來就只有 `sec` 這麼長(算完就沒了),
  取樣會一路響到錄音結束。不收的話 Freddie Green 的四分音符會全部疊在一起,
  一小節四顆和弦 × 三條弦 = 十二條弦同時在響
- 電平幾乎沒變(同一個 gain 下取樣比 KS 大 0.8–1.9dB),只有 bossa 的尼龍弦
  要從 0.58 收到 0.52——它最大聲,不收整組 bossa 的 RMS 會出目標帶

### 風琴、鋪底、打擊小物還是算出來的,而且**算完要存起來**
這幾層一個檔案都不抓。但「合成」不等於「每次現做」——下面這幾條是**流暢度**的規則:

- **撥弦(備援用的 KS)算完存成 buffer**(`ksBuffer`,key 是「音色:midi」)。
  **一定要在 `startPlayback` 裡預先算好**(`prewarmGuitar`):一顆 1.9 秒的弦
  要跑八萬次迴圈,排程中途算會卡在音訊的前 0.15 秒裡,聽起來就是一聲爆音
- **吉他取樣也在按下播放之前載完**(`prewarmGuitar` 回 promise,
  進 `startPlayback` 的 `Promise.all`),而且**一顆一顆排隊**:
  `decodeAudioData` 是主執行緒的工作,十四顆 mp3 一起解碼會跟排程器搶執行緒——
  邊播邊載的那一版 CPU 降速 6× 時排程一跳從 46ms 跳到 73ms 還開始「來不及」(踩過)。
  使用者說過「不能打開就用也沒關係」,所以這個等待是被允許的
- **取樣蓋得到的那幾顆就不要算 KS**(`prewarmGuitar` 先 await 取樣再補 KS)。
  KS 只是備援,用不到就不要花那八萬次迴圈
- **「這顆音要用哪一份取樣」要 memo**(`gtrPick`):刷弦一小節十幾下、
  放克的切音更多,每一下掃一遍 buffer 的 key 是白花的。載到新取樣時整個清掉
- KS 要像吉他,三件事不能省:**撥弦位置的梳狀濾波**(減掉延遲 1/5 週期的自己,
  少了它就是合成器的 pluck)、**激發先低通**(尼龍暗、撥片亮)、
  **迴圈裡的一階低通**(高頻先衰減,弦才會越響越暗)。
  音準用 `playbackRate` 修(延遲線長度取整數,再乘「整數長度 ÷ 真正的週期」),
  不要在迴圈裡塞全通濾波器
- **風琴與鋪底用 `PeriodicWave`,一顆振盪器就有整組泛音**。
  Hammond 的音栓本來就是固定頻譜,拿四五顆正弦去疊是四五倍的節點
- **雜訊只做一份**(`noiseBuf` / `noiseSrc`)。原本每打一下 hi-hat 就 `createBuffer`
  一次,放克一小節十六次配置。改成開場做一份兩秒的、每次從不同位置起播
- **808 的金屬音色(hi-hat / ride / crash)也先算好**(`hat808Buf`)。
  原本每一下開六顆方波振盪器,K-Pop 一小節就是九十六顆,
  CPU 降速六倍時排程一跳要 80ms;改成 buffer 之後降到 46ms
- **雜訊與 KS 的激發都要用固定種子**,跟 `hash01` 同一條規則。
  `Math.random()` 的話每次重新整理音色都不一樣,量到的尖峰也跟著飄
  (踩過:同一段 K-Pop 兩次量出 0.93 與 1.19,查很久才發現是雜訊不一樣)
- **每一顆合成音的起音都要有 1–1.5 毫秒的斜坡**。限幅器的 attack 是 1 毫秒,
  比它還快的爆點整個穿得過去,疊在已經在限幅的訊號上就衝破 1.0。
  加上斜坡之後 K-Pop 超過 0.9 的尖峰從 9 次掉到 2 次(量過),聽感完全沒差
- 加東西進去之前先量:**CPU 降速 4× / 6× 跑一輪**,看「排程一跳最久幾毫秒」
  與「有沒有來不及」(下一拍該響的時間已經過去)。
  加了真吉他取樣與 K-Pop 的低音鋪底之後,6× 之下最久 **61–71ms**、
  **來不及 0 次**、FPS 44–60(4× 是 21–45ms)。
  比原本的 46ms 高,那是新的兩層真的成本;**排程提前 150ms**,
  所以 70ms 的一跳還追得上——判斷標準是「來不及幾次」不是「幾毫秒」,
  4 次 × 366 跳都是 0。這個數字要往上再加東西之前先想清楚
- **這個量法本身很吵**,同一份程式碼跑四次差 10ms 以上,單獨一次量到的
  離群值(量到過 98.6ms)不要當結論——跑三四次看中位數

## 音色跟著曲風換,但那不是第四個軸
使用者**不能單獨選音色**,也不要加那個選單。「這首要練什麼」已經由曲風回答了,
再開一個「用哪台琴」只會組出說不通的搭配(808 打 bossa、平台鋼琴配 trap)——
跟當初把「曲風」做成必選選單被打回來是同一件事。

| 曲風 | 鍵盤 | 鼓 | 貝斯 |
| --- | --- | --- | --- |
| Pop | Salamander | 爵士鼓組 | 電貝斯 |
| City Pop | **Rhodes** | 爵士鼓組 | 電貝斯 |
| K-Pop | **合成 pluck** | **808** | **808 合成低音** |
| Bossa | Salamander | 爵士鼓組(cross stick) | 低音提琴 |
| Blues | Salamander | 爵士鼓組 | 電貝斯 |
| Swing | Salamander | 爵士鼓組(ride) | 低音提琴 |
| Funk | **Rhodes** | 爵士鼓組 | 電貝斯 |

(第四層——吉他、鋪底、打擊小物——見上面「伴奏團不是只有鋼琴鼓貝斯」那節)

- 四者都寫在 `FEELS[x]` 裡:`piano` / `drums` / `kit` / `parts`,沒寫就是預設那一套。
  **不要在排程裡針對某一種曲風寫 if**——跟 `feelFitsMeter` 同一條規則,
  第三個特例進來時一定會漏掉一邊
- **換的只是誰在彈**。`buildVoicing`、`schedulePiano`、`COMP_CELLS` 一個字都不動;
  音色跟聲位是兩件事,混在一起改就再也分不出問題出在哪
- Funk 的鼓**維持實錄爵士鼓組**:真的放克就是真人打真鼓,只有鍵盤換成 Rhodes。
  不要順手把它也改成 808
- **播放中換曲風,音色要在背景跟上**(`updateFeel` 的那三行):不要 await,
  擋住的話下一拍就沒聲音;那一台琴還沒到位時 `playNote` 會先用平台鋼琴頂著

## K-Pop 的四件事:它跟別人不一樣的地方全在音色
被回報過「聽起來不像 K-Pop」。第一版是 half-time 的節奏配 **Rhodes + 弦樂**——
節奏對了,但那個配器是**九〇年代 R&B**,放在 808 上面整個年代錯亂。
查了現在的 K-Pop 製作之後改掉四件事,缺一件就不像:

1. **和聲樂器是合成 pluck 不是電鋼琴**(`PIANO_KITS.pluck`):
   兩顆差七音分的鋸齒過一顆會關起來的低通(5kHz 掉到 700Hz,Q 開到 5)。
   **濾波器的包絡比音量的包絡重要**:只有音量衰減會得到一顆悶悶的鋸齒,
   要讓亮度跟著掉下來才有「叮」的感覺
2. **鋪底是 supersaw 不是弦樂**:疊三顆、失諧拉到 ±13 音分。
   兩顆差五音分聽起來只是「有點抖」,不是 supersaw
3. **808 低音要滑音**(`last808`):前一顆還在同一段落裡的時候,
   這一顆是從前一顆的音高**滑過來**的,不是重新彈一次。
   沒有滑音的 808 只是「一台很低的合成器」
4. **hi-hat 要滾奏**(`FEELS[x].roll`):偶爾一格變成兩三顆更快的音、力度往上推。
   **位置要挑**:落在「一拍的最後一格」(往下一拍衝過去的那一下),
   亂撒的話一小節冒出六七次,聽起來是故障不是律動。
   機率不到兩成,大概每兩小節一次

## 「不夠飽滿」是頻譜上的洞,要量出來才知道在哪一段
被回報「K-Pop 的聲音不夠飽滿,太單薄了」。**不要憑感覺去調音量**——
把每個曲風跑 24 秒、按八度頻帶量平均能量(scratchpad 的 `spec.mjs`),
洞就直接指出來了:

| | 30 | 60 | **120** | 250 | 500 | 1k | 2k | 4k |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Pop | −14.7 | −6.0 | **−3.0** | −9.0 | −12.2 | −19.2 | −22.7 | −27.0 |
| Funk | −22.9 | −6.9 | **−5.9** | −4.5 | −9.0 | −13.6 | −21.3 | −26.0 |
| K-Pop(修之前) | −10.0 | −10.0 | **−14.9** | −3.3 | −7.9 | −10.0 | −14.7 | −24.0 |
| K-Pop(現在) | −13.0 | −12.1 | **−6.8** | −3.4 | −9.2 | −11.3 | −15.8 | −25.4 |

- **120–250Hz 比別人低 10dB,那就是「單薄」的定義**。808 的正弦全部擠在 30–60、
  pluck 從 250 才開始,中間那段「身體」沒有人在。K-Pop 的鋼琴左手被壓到 0.3、
  又刻意不給吉他,所以那一段是空的——**是少一層,不是哪一層太小聲**
- **補的方法是加一層,不是把 keys 調大**(`FEELS[x].parts.klow` / `playKeysChord` 的
  `low`):鋪底的和弦往下移一個八度、**只取最低兩顆**,切在 110–420Hz 之間。
  上面切 420 是不要跟 pluck 撞,下面用高通切 110 是**一定要留給 808**——
  不切的話兩層低頻相加,尖峰直接爆掉
- **低音區的失諧與 LFO 都要拿掉**:低頻的拍頻很慢很明顯,照 supersaw 的 ±13
  疊下去是一秒抖兩下的嗡嗡聲;Leslie 一掛上去就變成會呼吸的低頻,跟 808 的側鏈打架。
  所以 `low` 一顆音只用一顆振盪器、不接 `slowLfo`——**省下來的節點正好是它的成本**
- **低音區只放兩顆音**。三顆是把泥巴堆起來,真的混音裡「身體」那一層永遠是一個雙音
- **808 也要有諧波層**(`playBassNote` 的 `h`):真的 808 混音一定過飽和,
  不是為了破音,是純正弦在手機與筆電喇叭上根本放不出來,要靠諧波讓耳朵補出基音。
  一顆**跟著滑音走**的鋸齒,用 100–460Hz 的帶通切出來。
  原本只有一顆 0.085 的三次諧波,而且**它不跟著滑音走**(音頭會分岔)。
  但它一個人補不起那個洞(808 是半拍子的、又被側鏈壓著),主力是上面那層 `klow`
- 這一節的教訓:**「聽起來很薄」「聽起來很吵」都是頻譜的事,量了再改**。
  憑感覺調音量會兩邊來回走——之前把 ride 收小又調回來就是這樣浪費掉的

## 每個曲風要有一個「一聽就知道」的聲音,而且不能被蓋住
被回報過「除了 City Pop,其他連風格都聽不出來」。查下去不是節奏寫錯
(把每個曲風的 onset 印出來,clave、shuffle、ride 打型、half-time 全部都在),
是**那個招牌被平均分布的高頻蓋掉了**,加上**速度沒跟著曲風走**。

- **速度是曲風的一部分**。原本的規則是「不會自己去動使用者打的數字」,
  那是只有三種曲風的時候訂的;現在七種橫跨 55–180,同一個數字下
  Bossa、Swing、Blues、Funk 聽起來會像同一件事。
  **點曲風膠囊就把速度帶到那個曲風的中速**(`setFeelPick` 裡),
  點「自動」與和弦改變導致的自動判斷不算——那不是使用者在指定曲風
- **不要讓兩層平均分布的高頻疊在一起**。這是這次最大的錯:
  bossa 的 hi-hat 打八下 + cabasa 打同樣八下 = 十六顆平均的高頻,
  蓋在三顆 clave 上面,clave 就不是 clave 了。放克的鈴鼓走八分疊在十六分 hi-hat 上
  也一樣,把十六分的強弱差抹平——而那個強弱差正是放克的律動來源。
  打擊小物要嘛跟 backbeat 一起(強化)、要嘛比鈸小一個量級(當空氣)
- **招牌的那一顆要在最上面**:Bossa 是框邊敲擊(0.54)與尼龍弦吉他,
  hi-hat 收到 0.13 只當氣音;Swing 是 ride(0.40);Blues 是 shuffle 的 hi-hat(0.32)。
  之前為了「不吵」把 ride 收到 0.30、Blues 的 hi-hat 收到 0.24,
  結果是整個曲風分辨不出來——**吵不吵靠強弱差(skip note 0.46、後半拍 0.45),
  不是靠把總量壓掉**

## 段落的角色:主歌 / 導歌 / 副歌
`SECTIONS` 每一筆都帶 `role`(`verse` / `pre` / `chorus` / `form`),
段落庫照角色分區,再加一顆「組一首」把主歌×2 →(導歌)→ 副歌×2 一次接起來。

- **角色是這組進行本身的性質,不是使用者選的**:`C - G - Am - F`(Axis)天生是
  副歌的形狀、`Am - G - F - E7`(安達魯西亞)天生是主歌的形狀。
  跟 `feel` / `meter` 一樣寫在 `SECTIONS` 裡,漏填不會報錯
- `form` 是「它本身就是一整首」(12 小節藍調、爵士標準曲的 A 段)。
  這種曲風沒有主副歌之分,**「組一首」那顆按鈕要整個收起來**,不要硬湊
- **分區從「4/8/12 小節」改成角色**:使用者要的是「先挑主歌再挑副歌」,
  長度是挑完才看的事(而且膠囊上一排幾顆和弦就是幾小節,本來就看得到)
- 按鈕的字樣要照**真的會接出來的那一串**寫,不要寫死「主歌×2 → 副歌×2」:
  有導歌的曲風接出來是五段
- 形式那一行寫 `主歌×2 ・ 導歌 ・ 副歌×2`,不是 `A A B B` 也不是
  `主歌 主歌 導歌 副歌 副歌`——連著重複的收成數字才塞得進曲風那排的右邊
- **小節數擺在形式前面**:那一行放不下時是從尾巴切掉(`.style-bar .bars` 的 ellipsis),
  最該看到的要在前面
- **`.style-bar .seg-mini` 一定是 `flex-wrap:nowrap`**:加了形式那一串之後,
  680–720 寬會把第七顆膠囊擠到第二排、整頁多 26px。要縮的是右邊那行狀態字,
  不是要點的按鈕(手機另有一套格線,不受這條影響)

## 現成的和弦進行
- **全部維持 C 大調 / A 小調,不要放別的調**。這是拿來練的,調性固定才好上手;
  要換調有移調滑桿。曾經散到 F / Em / G / Bb,被打回來過
- **每一組都要是真的常用的完整進行,不要湊數**。四小節那組要「自己循環得起來」——
  繞回第一小節要接得順;八小節那組要是完整的一句,最後一顆接得回開頭。
  `F - G - Em - Am` 就是被退掉的例子:它是王道進行(4536251)的前半,
  單獨當四小節循環並不完整,那一整句要放在八小節那組
- **每一組進行都帶自己的曲風(`feel`)與拍號(`meter`)**,點下去兩個一起換。
  曲風跟拍號一樣是推不出來的:丸サ進行是 City Pop 不是 Bossa、`C7 - F7` 是 Blues
  不是「直的屬七」,和弦看不出差別。所以 `SECTIONS` 的每一筆都要填 `feel`,
  漏填的那組點下去曲風不會換,而且**不會報錯**——很難發現
- 目前 74 組,按曲風分頁:Pop 23、City Pop 11、K-Pop 7、Bossa 8、Blues 6、
  Swing 14、Funk 5。同一個曲風裡再按「長度 · 拍號」分區
- **City Pop 不要停在小調 ii-V**。被回報過
  `Cmaj7 - Cmaj7 - Bm7b5 - E7 - Am7 - Am7 - Dm7 - G7` 聽起來不像 city pop:
  語彙沒錯(Plastic Love 裡就有小調 ii-V),**錯的是速度**——
  一顆和弦撐兩小節是爵士抒情的寫法,city pop 的和聲**一小節換一次以上**。
  改成 `Cmaj7 - Fmaj7 - Bm7b5 - E7 - Am7 - D7 - Dm7 - G7`:
  同樣有小調 ii-V,但它只是經過,整句一直在走
- **♭VII 與「整串都是 ii-V」都會把 `keyOf` 帶走**:
  `Cmaj7 - B♭maj7 - Fmaj7 - G7` judged 成 F 大調(B♭ 與 F 都指向 F)、
  `Em7 - A7 - Dm7 - G7` 判成 G 大調(從頭到尾沒有主和弦)。
  想加這種色彩就要讓主和弦在裡面站得住
- **K-Pop 的 `G` 刻意留三和弦**,不要照「能用七和弦就用七和弦」那條改成 `G7`:
  K-Pop 的 V 是純大三或 sus4,加屬七會變成藍調式的解決,而在 `…Cmaj7 - G` 繞回
  `Am7` 的循環裡那個三全音是往錯的地方拉。要七和弦的顏色交給「和弦變化」
  (它會給 `G6` / `Gadd9` / `G/B`)
- **分組看的是「幾小節」不是「幾顆和弦」**(`partGroups(e.text).length`):
  Satin Doll 那組是 8 小節 16 顆和弦,照顆數算會被丟進「4 小節」那一區
- **接段落不能直接用「-」串起來**(`applyForm` 走 `partGroups` + `groupsToText`):
  只要有一段裡面有 `|`,整串就會被當成小節線的寫法,前面那幾段的 `-`
  會變成「同一小節裡的和弦」,四小節被擠成一小節
- **有幾組是真的曲子的和聲**,改到它們之前先知道那是什麼:
  Swing 的 `Am7 - Dm7 - G7 - Cmaj7 - Fmaj7 - Bm7b5 - E7 - Am7` 是 Fly Me to the Moon 的 A 段、
  `Cmaj7 - Cmaj7 - D7 - D7 - Dm7 - G7 - Cmaj7 - Cmaj7` 是 Take the A Train、
  `Dm7 G7 | …` 是 Satin Doll、`Cmaj7 Am7 | Dm7 G7 | …` 是 rhythm changes 的 A 段;
  Bossa 的 `Cmaj7 - Dm7 - G7 - Em7 - A7 - …` 是 Laufey「From the Start」那一句、
  `Am7 - Am7 - Dm7 - Dm7 - Bm7b5 - E7 - …` 是 Blue Bossa 的前八小節;
  Swing 的 `Dm7 - G7 - Cmaj7 - Fmaj7 - Bm7b5 - E7 - Am7 - A7` 是 Autumn Leaves 的 A 段
- **整串都是 ii-V、從頭到尾沒出現主和弦的進行不能單獨收**:`keyOf` 會判錯調
  (Satin Doll 只取前四小節會判成 G 大調),整組代理規則跟著錯位。
  要收到主和弦它才站得住
- **同一個曲風裡不可以有兩組「只差聲位」的進行**。差別如果是「和弦變化」自己會做的
  (七度 vs 九度、Am vs A7、G vs G/F),那就不是兩組進行,是同一組的兩種彈法,
  只該留一組——**留七和弦那一版**(被指出「POP 那組實際上就是 City Pop 那組」)。
  拿掉過兩組:Pop 的王道(跟 City Pop 那組只差第 2 格的 G/F vs G7,整組搬去 City Pop)、
  Swing 的 `Dm7 - G7 - Cmaj7 - Cmaj7`(跟 `Dm9 - G13 - Cmaj9 - Cmaj9` 只差聲位)
- **跨曲風的同一串根音是可以的,那是特色不是重複**:`C - Am - Dm - G`(Pop)與
  `Cmaj7 - A7 - Dm7 - G7`(Bossa)是同一句歌用兩種語彙彈,拍號、律動、聲位都不一樣,
  練的是兩件事。只有「同一個曲風裡」才算重複
- **Pop 的進行維持三和弦**,不要整批改成七和弦:那會讓 `detectFeel` 把它們判成 Bossa
  (六七度過半),使用者自己打 `Cmaj7 - G7 - Am7 - Fmaj7` 就得不到 Pop 的伴奏。
  想要七和弦的聲音,「和弦變化」會自己加上去(`C → Cmaj7`、`F → Fmaj7`)
- **`SECTIONS` 只帶 `text` / `feel` / `meter` 三個欄位**。不要再加「建議的疏密」回去,
  那是使用者那一軸的(見上面「兩個軸」)
- Pop 的王道進行用七和弦聲位(`Fmaj7 - G/F - Em7 - Am7 - Dm7 - G7 - Cmaj7 - C7`):
  `G/F` 是屬七的三轉,低音 F→E 順著下行接到 Em7,那是這句最常見的寫法
- **哪個曲風配哪些拍號是有答案的**:圓舞曲與 6/8 抒情都是 Pop(換的是拍號不是曲風)、
  City Pop 與 Funk 只有 4/4、Blues 只有 4/4 與 6/8(12/8 慢藍調)、
  爵士圓舞曲是 Swing 的 3/4。不要為了湊滿 6×3 去補不存在的組合
- **爵士的 3/4 與 6/8 要用九度以上的聲位**,不然 `detectFeel` 判不到搖擺:
  `Cmaj7 - Am7 - Dm7 - G7` 四顆裡有三顆是六七度,會被判成拉丁(afro waltz),
  寫成 `Cmaj9 - Am9 - Dm9 - G13` 才是爵士圓舞曲。慢藍調同理:
  `C7 - F7 …` 是直的 6/8、`C9 - F9 …` 才會走 12/8 的 shuffle ride
- 膠囊上只顯示和弦,不要寫風格名字(註解裡可以寫,那是給改程式的人看的)
- **循環進行不要硬撐成八小節**。丸サ進行(`Fmaj7-E7-Am7-C7`)最後那顆 C7 是
  「把 Fmaj7 當暫時主和弦」的屬七,`Vm7-I7-IVM7` 等同 ii-V-I,所以它天生就是
  四小節一直繞。曾經放過一組「第八小節停在 Am7」的八小節版,那不是真的曲子的寫法,
  已經退掉;要八小節按 ×2 就好。八小節那格改放王道的七和弦聲位
  (`Fmaj7-G7-Em7-Am7-Dm7-G7-Cmaj7-C7`),那才是真的八小節一句

## 編輯流程:先定長度,再一格一格改
使用者要的流程是「先選 4 / 8 / 12 小節 → 點第 1 格改它 → 點第 2 格改它」,
不是「點音名就往後長一格」。所以:

- **`lenSeg`(4 / 8 / 12 小節)是進行的長度,不是段落庫的分類**。按下去把進行
  變成 N 格:變多就把現有的重複貼滿(4 → 8 就是同一句唱兩次)、變少就截掉,
  然後停在第 1 格。亮的那顆是「現在幾小節」
- **一直有一格在編輯**。點格子換一格,點同一格不會取消(取消了鍵盤就不知道
  要改誰);沒選到時 `targetBar()` 當第 1 格
- **點音名/性質只改正在編輯的那一格,不會自己往後跳**。改完第 3 格還是停在
  第 3 格。一格都沒有的時候才開一格出來
- 鍵盤:`←` `→` 換上一格/下一格,數字鍵 `1`–`9` 直接跳到那一格
  (焦點在輸入框裡時完全不攔)
- 段落庫**不能**再放一個「4 小節 / 8 小節」的切換:那個標籤跟上面的「小節數」
  撞在一起(一個改進行長度、一個換清單),一定會按錯。清單改成兩組一起列,
  中間插一條小標
- **段落庫的分頁是曲風,不是拍號**。使用者想的是「我要練 City Pop」,
  不是「我要練 4/4」——拍號是進行自己帶的,點下去就換好了,不必先選。
  分頁的名字跟最上面那排曲風**一模一樣**
  (Pop / City Pop / K-Pop / Bossa / Blues / Swing / Funk),
  兩邊對得起來才看得出「這一頁就是那一種」

## 編輯就在小節格旁邊
和弦進行有三個地方可以改:文字框、和弦鍵盤、小節格。它們動到的是同一串文字
(`barTokens()` / `setTokens()`),所以要擺在一起,不要散在兩欄的兩端。

- 左欄的順序是 **輸入 → 小節數/段落庫 → 小節格 → 播放 → 和弦鍵盤(含小節工具列)**;
  右欄只放伴奏設定(樣式、速度移調、開關、存檔)。
  和弦(小節格)跟播放鈕在上面、選字母與性質的鍵盤在下面:上半是「這首曲子長怎樣、
  按下去聽」,下半是「要改就在這裡改」。播放鈕從右欄搬到左欄就是為了這個順序,
  手機疊成一欄時也是同一串
- 走帶搬進左欄等於把 58px 加在本來就比較長的那一欄,720 高的筆電放 12 格、32 格
  就爆了。補回來的方法寫在下面「版面」那節(clamp 的係數要讓 720 取到下限、
  兩欄螢幕的小節格再矮一階),不要靠把播放鈕搬回右欄解決
- 小節工具列併在鍵盤的底列(`.pal-foot`),不要自己佔一排:那一列本來就有
  「正在改第幾格」的提示,而且獨立一排會多 35px。按鈕一直都在、沒選取時停用,
  不然一選取就多一排、整個版面會跳
- 工具列**只有三顆**:插一格(複製正在編輯的那一格)、刪一格、復原。
  曾經還有 `◀ ▶` 搬移,砍掉了:四小節的練習循環很少需要換順序,
  真要換順序在文字框裡改比較快,而五顆圖示鈕擠在一排反而看不懂。
  加按鈕之前先問「這件事沒有它就做不到嗎」
- **復原**存的是整串文字的前一版(`pushUndo()`),鍵盤、段落庫、工具列、清空、
  改長度都要先呼叫它。文字框自己打字不用經過(瀏覽器有內建的復原),
  ⌘Z / Ctrl+Z 在焦點不在輸入框時才攔

## 空出來的版面拿來放內容,不是拿來放大按鈕
平板上卡片浮在中間、上下各空 200-300px,被打回來兩次。兩條路都試過了:

1. **把按鈕放大**(--grow 開很大)→ 按鈕變成怪物,而且還是空
2. **把卡片撐滿、縫拉開**(`min-height:100vh` + `margin-top:auto`)→
   一個接縫被拉開將近 200px,版面整個散掉

對的做法是 **3. 多出來的高度給「會長的東西」**:段落庫是清單,多高都合理。
螢幕夠大(`min-width:720px` 且 `min-height:900px`)時:

- `.card` 給**定高** `calc(100vh - 148px)`、`grid-template-rows:auto 1fr auto`,
  段落庫那一列是 `1fr`。給定高不是 min-height:格線的 `1fr` 要有確定的高度
  才收得住,不然清單會照自然高度長出去、整頁被撐破
- `placeSecPanel()` 把 `#exPanel` 從左欄的浮動位置搬到卡片底下的 `#exInline`
  (整排),`body.sec-inline` 帶動樣式;視窗變化時再搬回來
- 這樣 1366 寬的平板橫放一次看得到 17 組進行、13 吋直放看得到全部 24 組,
  上下留白只剩 20px
- `--grow` 同時**調小**(`clamp(0px, calc(2.2vh - 16.1px), 16px)`):
  元件只長一點點,空間留給清單
- 門檻不能再低:720×900 已經是極限(32 小節時清單只剩 49px),
  680 寬或 820 高再放內嵌清單,12 小節就會把整頁撐破(實測 +13px)
- `--grow` 動過就要重量 **4/8/12/16/24/32 小節 × 14 種尺寸**,一個都不能漏
- 速度與移調那兩塊要用 **container query**(`container:ctl / inline-size`)
  擋住圓鈕長大:平板直立時一塊只有 156px 寬,44px 的圓鈕會把滑桿擠成一個點、
  刻度 −12 / 0 / +12 疊在一起。條件寫 `.control .step-btn`(兩層)才蓋得過
  後面 `@media` 裡的 `.step-btn`
- 不要改 `.control-pair` 的 `minmax(152px,1fr)` 來解這件事:改成 168px 的話
  手機(卡片寬 330px)會掉成一欄,整頁高度多 128px

## 段落庫是浮動面板,預設收起來
- 13 + 11 組清單展開有 200px 出頭,一直佔著左欄快一半的版面——多數時候
  使用者只是要看自己那一段,所以預設收起來,開關上寫著有幾組
- 面板 `position:absolute` 浮在卡片上,**不要把下面的東西推開**:一推開整頁就爆掉
  (實測展開會超出 150px 以上),而且看完就收起來了,本來就不該佔位置
- 清單高度上限由 `fitSecPanel()` 在打開時量:「從清單上緣到視窗底還有多少」。
  純 CSS 的 `min(52vh, 430px)` 不夠——面板雖然是浮的,但比視窗剩下的高度長的話
  整頁還是會被撐出捲軸(24 組一起列之後踩到)
- 清單的結構是**一行標題 + 按「長度 · 拍號」分區**:標題(`#secTitle`)只講
  「現在列的是哪一個曲風的幾組」,要換曲風是點卡片最上面那排。
  下面依序是「4 小節」「8 小節」「12 小節」,同一個長度裡再按拍號拆開。
  長度分區是因為「我要練四小節的循環」跟「我要練一整句」是兩件事,
  混在一起找不到(36 組一路排下來被退過)
- **標題只在那個曲風橫跨兩個以上拍號時才寫拍號**(`secLenMeterGroup`)。
  Bossa 全是 4/4,寫「4 小節 ・ 4/4」是廢話;Pop 的 3/4 是圓舞曲、6/8 是抒情歌,
  那是兩種不同的歌,不寫的話點下去才發現拍號變了
- **曲風真的變了才重畫清單**(`updateFeel` 裡比的是「跟剛才一樣嗎」):
  每次打字都重畫的話,捲到一半的清單會一直跳回頂端
- 內嵌的時候一排排得下四五組(`minmax(176px,1fr)`),八小節的佔兩格、
  12 小節的佔三格(`.long` / `.xlong`)——整排 1000px 只放一組太浪費
- 正在用的那組會亮起來(`.ex-btn.on`,比對 `formParts`):清單一直開著的時候
  要看得出來自己在練哪一個
- `.examples` 的 `overflow-y:auto` **不要放在 `@media (min-width:680px)` 裡**。
  手機沒有那條規則的話,36 組會整串攤開把頁面拉長一千四百多 px(踩過)
- 點面板外面或按 Esc 收起來;點面板裡面(挑段落)不關,才接得出 A A B B。
  整個 `.ex-wrap` 都算「裡面」,不然按「清空」想重接一次形式時面板會跟著關掉

## 移調時畫面要顯示真的在響的和弦
- 文字框留使用者自己打的那一版不動(那是原稿),小節格與走帶列顯示移調後的名字
  (`chordDisplay()`):`C - F - G - C` 移 +2 就顯示 `D - G - A - D`
- **拼法要照樂理走,不能拿「pc → 固定音名表」硬換**。+1 是「上小二度」,字母要往上
  走一格:C 變 `D♭` 不是 `C♯`。照表換的話 `Dm7-G7-Cmaj7-Fmaj7` 會變成
  `E♭m7 - A♭7 - C♯maj7 - F♯maj7`——同一段裡升降混用、四度的字母鏈也斷了,
  正確的是 `E♭m7 - A♭7 - D♭maj7 - G♭maj7`(D♭ 大調)。被抓過一次
- 走幾格字母是**整段一起決定**的(`transposeSteps()`),不是一顆一顆各自算,
  不然同一段裡會出現 `C♯maj7` 跟 `E♭m7` 並排。本位走法之外也試同音異名的兩個,
  用「有沒有重升重降 / 有沒有 C♭ F♭ B♯ E♯」計分挑最順的那個:
  `Bb - Eb - F - Bb` 上小二度照算是 `C♭ - F♭ - G♭ - C♭`,計分之後會寫成 B 大調
- `parseChord` 除了 pc 還要留原本寫的**字母**(`rootLetter` / `bassLetter`)、
  性質(`suffix`)與有沒有轉位低音(`slash`):只有 pc 拼不出正確的音名,
  也組不回完整的和弦名
- 性質裡的升降也排成正式記號(`prettySuffix`):`m7b5` → `m7♭5`、`7#11` → `7♯11`。
  只換「記號後面接數字」的,`add9` / `sus4` / `alt` / `It+6` 都不會被動到。
  根音已經是 ♭ ♯ 了,性質還留 ASCII 的 b 會很難看
- 小節數那一列會補一句「下面是移調 +2 的和弦」。不講的話會以為文字框沒跟著改是壞掉了
- 標題右邊那一行(`updateBarCount()`)一次把**形式、小節數、移調**講完,不要再另外開一行。
  曾經有一條 `form-line` 寫「自由輸入 ・ 12 小節」,跟上面的小節數是同一句話,
  還多佔 22px
- `setTranspose()` 要重畫小節格。播放中重畫沒關係:下一幀 `paintPlayhead`
  會把正在響的那一格重新點亮

## 練習用的小東西
- **複製連結**(`shareURL()` / `applyURLParams()`):和弦、速度、拍號、樣式、移調
  編成 `?c=…&bpm=…&m=…&s=…&t=…`。老師把一條網址傳給學生,對方打開就是同一題。
  **網址有參數就蓋過 localStorage**——傳連結的人要的就是「對方看到的跟我一樣」
- 加到主畫面會用全螢幕開(`apple-mobile-web-app-capable`),平板架在譜架上不會被
  網址列吃掉一條。**沒有 service worker**:那要另外一個檔案(單檔 HTML 的規則),
  而且取樣在 CDN 上,真要離線得連取樣一起快取,是另一件事

## 拿掉過的
- **愈練愈快**(每兩圈 +5 BPM):做過,後來被要求拿掉。要加回去之前先問——
  它跟「和弦變化」一樣是在圈的第一拍動手腳,兩個一起跑會變成每繞一圈
  速度跟和弦都在變,練的人抓不到基準

## 被提過但**刻意不做**的
別人看到這個工具常會建議下面幾件,每一件都想過了:

- **當作入口的曲風選單**(打開就要先挑 Pop / Swing / Bossa / Ballad):做過,被打回來過。
  現在的「感覺」那排是**覆寫**不是入口——預設那顆是自動,自動判出來的才是常態,
  九成的人一顆都不用按。不要把它改回「使用者一定要先選一種」,也不要把自動拿掉
  (詳見「兩個軸」那節)
- **各軌靜音 / 混音器**(「貝斯手想把貝斯拿掉」):跟下面「鼓與貝斯永遠都在」直接衝突。
  那個需求是真的,但它等於要一個 minus-one 模式,不是加一顆靜音鈕就好——
  要做要先講清楚是不是要改掉「永遠都在」這條
- **MIDI / 音訊匯出**:MIDI 寫得出來(單檔、無相依),但這個工具的定位是「打開就跟著彈」,
  匯出是 DAW 的事。真的要做再說
- **小節區段循環**(只循環第 3–4 小節):有用,但要在小節格上加「選範圍」的互動,
  目前的小節格是「點一格編輯那一格」,兩種選取會打架。想清楚再做

## 鼓與貝斯永遠都在
沒有開關，不要再加回去。`lhGain()` 固定回 0.55（貝斯一直在，鋼琴左手要讓位）。

## 響度
拿來跟著彈的，音量不夠會被真鋼琴蓋過去。鏈路是
`masterGain ×1.9 → 限幅器(−10dB, 20:1) → outGain ×1.25 → 軟削波(0.72 起彎、天花板 0.96)`。
四段一起調，動其中一個就要重新量：目標 peak ≤0.9、RMS −12～−15 dBFS、零削波。
outGain 放到 2.4 會讓尖峰衝到 1.6 直接爆掉（實測過）。

- **最後那一關的軟削波不是補丁,是這條鏈本來就缺的天花板**。
  限幅器的 attack 是 1 毫秒,比它快的東西(鼓的音頭、撥弦、雜訊)本來就穿得過去。
  `WaveShaper` 在 0.72 以下完全是直線(正常的伴奏根本碰不到),
  以上用 tanh 彎進 0.96,所以**數學上保證**沒有取樣超過 0.96。
  **`oversample` 要是 `"none"`**:開 4× 的話升降取樣的濾波器自己會振鈴,
  輸出反而衝過天花板(實測還是量到 1.03)
- **「把某一層調小」常常讓尖峰更高**,這條反直覺但量過好幾次:
  大鼓本來在踩限幅器、順便壓住別人,一收小限幅器就放手,
  撐著的那幾層整個爬上來(K-Pop 把 808 調小 1.5dB 之後 0.98 → 1.04)。
  所以尖峰過高**先找「誰在撐著」**,不是找「誰最大聲」
- **808 的低音要側鏈**(`duckBass`):808 的大鼓與 808 的低音是同一顆音色,
  都是四五十赫茲的正弦,正弦的波峰因數是 1(所有能量都在尖峰上),
  相位一對上就直接相加。大鼓落下時把低音壓到 22%、130 毫秒後放回來。
  這不是為了修 bug——真的 K-Pop / trap 混音一定會做,那個「一呼一吸」就是它的味道。
  **只有 808 低音的曲風才套**,而且換走曲風時要把 gain 放回去
- **K-Pop 的鋼琴左手要再讓一階**(`FEELS.kpop.lh = 0.3`):808 sub 已經撐滿整小節,
  鋼琴再疊一層等於兩條正弦低頻相加。讓多少寫在 `FEELS[x].lh`,
  沒寫才回到「boogie 0.38、其餘 0.55」
- **量響度要看每一個取樣**,不要用 analyser 輪詢:輪詢會漏掉尖峰,
  同一段量兩次差到 0.14(踩過,追了很久才發現是量法的問題)。
  用 `ScriptProcessorNode` 接在軟削波之後,每一個 block 全掃一遍
- 現在量到的(七個曲風 × 推進/混合,每段跑 20–46 秒,**全部零削波**):
  peak 0.831–0.918、RMS −12.0～−15.2 dBFS。
  加了真吉他取樣與 K-Pop 的低音鋪底之後重量過:K-Pop 的 RMS 從 −13.2 升到 −12.0
  (那就是「變飽滿」),peak 沒動
- **搖擺比別人低 2dB 是對的,不要硬拉平**(推進 −15.0、打點 −15.2,
  同樣密度下 Pop 是 −13.1、Bossa 是 −13.7)。爵士本來就稀:大鼓是 feathering(0.16)、
  小鼓在 comping、沒有打擊小物也沒有鋪底。往上補的是**踏鈸(0.30)、comping 的小鼓(0.42)
  與 Freddie Green 的吉他(0.35)**——那三層是「拍子找不找得到」;
  **ride 維持 0.40**,它的位置是被回報「很吵」之後量出來的,往上就又糊了。
  補完也只換到 0.5dB,剩下的差距是這個曲風本來的樣子

## 版面
- 680px 以上分兩欄（左 `.col` 輸入、右 `.col` 播放），目標是整頁塞進一個畫面。
  改版面時拿 `document.body.scrollHeight` 量：1280×720 / 1280×800 / 1440×900 /
  1100×800 / 900×800 / 800×800 / 820×900 都要 ≤ 視窗高，而且 4、12、24、32 小節的
  進行都要一樣高（小節多就靠 `.bars-grid` 的 dense / denser 換小格）。
  平板與桌機量過的尺寸（720–1366 高、744–2560 寬）整頁都不用捲，
  段落清單放不下時是清單自己捲、不是整頁捲。只有手機是已知要捲的
- 斷點用 900 / 680 / 600 / 400，跟 ScrollScore 與首頁對齊，不要自己另訂
- 窄螢幕疊回一欄，兩欄的 DOM 順序必須跟單欄想要的順序一致
- 一排按鈕數量不固定時用 grid 不要用 flex：flex 會把最後一行剩下的幾顆撐滿整列，
  同一顆鈕在不同行寬度差一倍（音名列、開關列、範例列都踩過）。
  用 `auto-fill` 不要 `auto-fit`——`auto-fit` 會把空格子收掉，最後一行又被撐開
- 目前每一塊都是等寬格線：範例 2 欄、音名 6 欄、性質 7 欄（手機 5 欄）、小節格 4 欄、
  曲風那排在手機上 4 欄（七顆剛好 4+3;排三欄會變成 3+3+1,最後一排孤零零一顆
  而且多佔 34px）。
  加東西進去要跟著對齊，不要讓某一塊變成參差不齊的 flex
- 範例膠囊裡面也是格線：一顆和弦一個等寬格子、置中，跟「一個和弦一小節」對得上。
  四小節的段落一排兩個，八小節與 12 小節藍調一組佔一整排（`.ex-btn.long`），
  不要讓 8 顆和弦跟隔壁那組擠在同一排——看不清楚也容易點錯
- 膠囊一律有邊框加底色（`--surface-inset`），每一組的範圍要一眼看得出來；
  手機 `min-height:34px`，那是用手指點的
- **小節格一排四格**，因為樂句就是四小節一句：第 5 格一定在第二排的開頭，
  12 小節藍調剛好三排。超過 12 格才改成一排八格（等於一排兩句），第 4 與第 5 格
  之間開一點縫（`.bars-grid.eight .bar-chip:nth-child(8n+5)`）把兩句分開。
  **不要用六欄或兩欄**：六欄會讓第 5 格跑到第一排中間，看的人就數不出樂句了
- 右欄用 `justify-content:space-between` 把多出來的高度分掉。左欄（輸入＋編輯＋播放）
  本來就比右欄長，不分的話右下角會空一塊
- **功能的輕重**(被要求整個審一遍之後定下來的):
  最上面一排是**曲風**(這首要練什麼,全寬,也決定段落庫列什麼);
  左欄是**這首曲子**(和弦輸入 → 小節數/段落庫 → 小節格 → 播放 → 和弦鍵盤);
  右欄是**怎麼伴奏**(伴奏樣式 → 拍號+速度移調 → 開關 → 存檔);
  最底下(螢幕夠大時)是**段落庫**,那是會長的東西,多出來的高度給它。
  由上而下就是使用者真的會做的順序:挑曲風 → 挑進行 → 按播放 → 邊練邊調。
  拍號緊接著速度(`.tempo-group` 把兩塊綁在一起):6/8 的 60 是**附點**四分音符的 60,
  兩個要一起讀。不綁的話右欄用 space-between 分高度時會把它們中間撐開一道縫
- **拍號放右欄、貼著速度**,不要放左欄「小節數」那一排:那一排是
  `[4 小節][8 小節][12 小節]`,旁邊再放一個 `[4/4][3/4][6/8]` 一定會按錯
  (一個改進行長度、一個改一小節幾拍,長得又像)。
  而且拍號跟速度要一起讀——6/8 的 60 是附點四分音符的 60。
  左欄也早就沒有空間了(見「編輯就在小節格旁邊」那節的 58px)
- 拍號用 `.seg-mini`(小膠囊)不用 `.seg`(大按鈕):設定一次就好的東西
  不需要跟伴奏樣式搶注意力,而且大按鈕一排會多 51px
- **clamp 的 vh 係數要讓 720 高的螢幕剛好取到下限**(`Nvh × 720 ≤ 下限`)。
  寫 `clamp(4px,1vh,16px)` 的話 720 高會拿到 7.2px、比下限多 3px,一個元件三、四 px,
  整欄加起來就是 20px,剛好是 1280×720 放不放得下 12 格的差別。
  要撐高螢幕是靠上限,不是靠係數
- 兩欄的螢幕(`min-width:680px`)小節格可以再矮一階:`dense` 24px、`denser` 22px、
  間距 6px。手機維持 28 / 24 與 8px——那是用手指點的,不能跟著縮
- **高螢幕不要讓卡片浮在上面、下面空一大塊**(13 吋平板直立 1024×1366 踩過)。
  兩件事一起做:`@media (min-width:680px)` 裡讓元件與區塊間距跟著視窗高長大
  (`clamp(原尺寸, Nvh, 上限)`——短螢幕取到下限,完全不受影響),
  剩下的空間再用 `.stage{margin-block:auto}` 平均分到上下。
  用 margin auto 不要用 `justify-content:center`:空間不夠時後者會把上緣頂出捲動範圍
- 段落清單在浮動面板裡,高度上限 `min(52vh, 430px)`,跟其他版面無關。
  (曾經用 `calc(100vh - 607px)` 把它跟整頁高度綁在一起,搬過版面就要重量一次,
  改成浮動面板之後這條就不需要了)
- 清單捲得動的時候上下要有漸層(`.more-above` / `.more-below`,JS 依捲動位置掛),
  不然被切一半的那一排看起來像畫壞了。捲軸的 thumb 用 `--muted`,
  用 `--line` 在深色卡片上根本看不見
- `auto-fill` 的 `minmax()` 下限訂太大會在中間寬度掉成一欄，頁面一下子長一截：
  900–800px 那一段欄寬只剩 340–400px，範例是 `minmax(164px,1fr)`、
  速度移調 `.control-pair` 是 `minmax(152px,1fr)` 才留得住兩欄
  （原本 188 / 196，820px 時範例變 8 列、900px 時速度移調疊成兩列）。
  而且這裡會自己咬自己：一掉成一欄頁面就變高 → 冒出捲軸 → 欄又窄 15px →
  更掉不回去，所以量的時候要看有捲軸的寬度，不要拿理論值算

## 技術棧
- 單檔 HTML，CSS/JS 內嵌，CDN only，無 build，GitHub Pages 直接部署
- 配色/字體/元件樣式跟 ScrollScore、SightScore、LoudMaster 共用同一套變數，不要自己發明新的
- 一個和弦一小節；拍號 4/4 / 3/4 / 6/8 三選一（見下面「拍號」那節）

## 絕不能做的事
- 不要把單檔拆成多檔案，除非我明確要求
- 不要改用 setTimeout 排音符。排程一律走 `AudioContext.currentTime` 提前排，
  setInterval 只負責「每隔一陣子來看一下」，不決定任何一個音的實際時間點
- 不要把取樣改成一次載完整組；iPad 第一次開會等很久。只抓這次進行真的會用到的音
- 不要拿掉解析失敗時擋住播放的那一段。看不懂的和弦要明講是哪一個 token，
  不要自作聰明猜成別的和弦

## 已知踩過的坑
- SVGElement 沒有 hidden 這個 IDL 屬性，`el.hidden = true` 只會掛上一個沒人看的 JS 屬性，
  內容屬性不會變。播放/停止圖示一定要走 setAttribute / removeAttribute（見 `hideSvg`）；
  而且瀏覽器內建的 `[hidden]{display:none}` 綁在 HTML 命名空間上，對 `<svg>` 無效，
  要自己寫一條 `svg[hidden]{display:none}`
- 速度改變時不能直接拿舊的 anchorTime 來減，要重新錨定：讓「下一個還沒排的拍」維持原本
  該響的時間，新速度從那一拍之後才生效，已經排進去的音不會被改到一半（見 `reanchorTempo`）
- 正規化時不要動 `+`。寫 `/+/g` 是「Nothing to repeat」語法錯誤（整支 script 都不會跑），
  就算改成 `/[+]/g` 寫對了，`It+6` 也會被換成 `Itaug6`、`7+5` 變 `7aug5`，兩個都查不到。
  `+`、`7+5`、`It+6` 一律當字面 key 收在 QUALITY 表裡
- `/` 有兩個意思：`C/E` 是轉位低音，`6/9` 是和弦記號自己帶的斜線。只有斜線後面
  真的是音名時才當轉位，否則整串留著查表（用 lastIndexOf，`C6/9/E` 才切得對）
- 中文輸入法很容易打出 `Ｃ７` 這種全形字，正規化第一步就要把全形英數符號轉半形
- 六、七個音的和弦不要整組照彈。捨音順序：五音 → 根音（轉位時不能捨）→ 延伸音，
  上聲部最多四個音（見 `trimTones`）
- 和弦品質表的 key 大小寫有意義（`M7` 是大七、`m7` 是小七），不能整串 toLowerCase
- 每小節都要從所有轉位裡挑離前一小節最近的那個，不然 C→F→G 會整組跳上跳下，
  聽起來像機器在按琴鍵（見 `buildVoicing` 的 cost）
- Salamander 每個八度只錄 C / D# / F# / A 四個音，其餘用 playbackRate 補位。
  音域是 A0–C8，鋼琴全音域都蓋得到，低音區不需要另外處理
- 鼓是 FLAC，`decodeAudioData` 對 FLAC 的支援不如 mp3（舊的 iOS Safari 可能吃不下），
  所以每個鼓件都要留合成備援（見 `synthDrum`），不能假設取樣一定載得到
- 貝斯一律待在 E1–D#2（`bassMidiFor`）。移調只換音級、不跟著跑到別的音域，
  不然移幾度貝斯就飛到鋼琴中間、低音整個空掉
- 貝斯軌開著時鋼琴左手要降音量（`lhGain`），同一個根音疊兩層低頻會糊
- 三種伴奏樣式的左手低音都要留長（撐兩拍，墊底撐整小節），不要只響一個音就收掉
- 變化用 `hash01(絕對小節號, salt)`，不要改成 `Math.random()`：那樣連「這一拍到底
  加了沒」都無法重現，出問題沒辦法追
- `hash01` 一定要兩輪 murmur 收尾。第一版只混一輪，整體分布看起來沒問題
  （四分位都是 0.25），但把 salt 固定、小節號從 0 數上去會連出一串很小的值，
  實測貝斯前五小節全部選到同一條路。雪崩不夠就是會這樣，不要為了短而簡化它
- 過門的鼓件（htom / ltom / crash）加起來 2MB 出頭，要背景載、不要擋播放
  （見 `ensureDrums` 裡沒有 await 的那條）。還沒到位時 `synthDrum` 會頂著
- 一格的進行沒有「最後一格」可言，過門只在兩格以上才給
- 大段落搬移程式碼時，先確認 `hash01` / `chance` 這種被大家用到的小工具沒被一起搬走。
  整段換掉 `STYLE_FNS` 那次就把它們刪掉了，症狀是「取樣都載好了但按播放沒聲音」，
  而且 pageerror 抓不到（錯誤發生在 promise 的 .then 裡），要靠 window error 才看得到
- 和弦名字被切掉比字小一號嚴重得多：`Cmaj7` 切成 `Cm…`、斷行成「Cmaj / 7」都會被
  看成別的和弦。格子不夠寬時一律縮字（範例膠囊、小節格、性質鈕都是同一招：
  `font-size:min(原字級, calc(格子寬 ÷ 字數 ÷ 常數))`），常數是量出來的
  （Noto Serif 一個字約 0.73 個字高，取 0.76 留餘裕）。算的時候要扣掉自己的內距與
  邊框，`100cqi` 給的是內容寬、不含 padding，少扣一次就會剛好差幾 px 又被切到
- `@container` 的條件是對「最近的祖先容器」算的，不是寫在哪一塊就算哪一塊。
  `.examples` 與 `.ex-btn` 兩層都是容器時，`@container (max-width:340px){ .ex-ch{…} }`
  會拿 `.ex-btn` 的寬度去比，不是 `.examples` 的——踩過一次，症狀是 800px 寬時
  字級沒縮、和弦被切掉。所以容器一律取名（`exbox` / `exrow` / `palq`），
  條件也一律寫成 `@container exbox (…)`
- 同一排的字級要一致：範例膠囊的 `--len` 取「同樣長度的進行裡最長的和弦字數」，
  不要每一組各算各的，不然同一排字忽大忽小
- container query 是 2022 年以後的瀏覽器才有。老瀏覽器上 `100cqi` 整條 font-size
  會被丟掉，退回原本的字級（窄的時候會切字），這是可以接受的退化——不要為了它
  改回 media query，這一塊在單欄與兩欄的寬度差很多，看視窗寬度一定判錯
- 過門不要佔滿最後兩拍、也不要寫成整串 tom 滾奏：從八分突然跳成十六分很突兀。
  預設只佔最後一拍，hi-hat 要繼續走不能斷，力度從很輕推上去
